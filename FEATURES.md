# 기능 목록 (Feature Catalog)

**이 파일은 공개본 사본이다** - 정본은 이 저장소가 연동된
claude-native-workflow 설치의 `FT` 타입 문서(영역당 하나)다. 갱신할
땐 그 설치의 `FT` 문서를 먼저 고치고, 여기에도 같은 내용을 반영한다
(CLAUDE.md "이 저장소에 대하여" 절 참고).

이 문서는 claude-native-workflow(v2, `main` 브랜치)가 **만들고자 하는/
이미 만든 기능 전체**를 영역별로 정리한 카탈로그다. README.md(라운드별
변경 요약 + 실행 방법)·DESIGN-NOTES.md(라운드별 구현 기록)·`QA` 문서
(시나리오+QA 체크리스트)와 성격이 다르다 - 이 문서는 "지금 왜
바뀌었나"나 "어떻게 검증하나"가 아니라 **"이 시스템이 결국 무엇을
하는가"**를 한 곳에서 훑어보기 위한 참고 문서다.

새 기능을 추가하거나 기존 기능을 바꾸면 이 문서도 함께 갱신한다(해당
영역 항목을 고치거나, 새 영역이면 절을 추가). 각 항목은 "무엇을
하는지"만 간결하게 적는다 - 구현 세부사항·버그 수정 이력은
DESIGN-NOTES.md에, 검증 절차는 `QA` 문서에 남긴다.

---

## 1. 인증 & 신원 관리

- 회원가입/로그인/로그아웃, JWT(단명 access + 장기 refresh 토큰 회전).
- 최초 설치 시 계정이 하나도 없으면 관리자 계정을 `admin`/`12345678`로
  자동 시드(이미 계정이 있으면 건드리지 않음).
- **로그인 무차별 대입 방어** - 아이디/이메일별(5회/15분), IP별(20회/
  15분) 이중 잠금. 어느 한쪽이든 초과하면 15분간 그 조합의 로그인이
  전부 막힌다(비밀번호가 맞아도 잠겨 있으면 거부). 성공 시 그 계정의
  카운터만 초기화(같은 IP의 다른 계정 spray 방어는 유지). 429 응답에
  `Retry-After` 헤더 포함.
- **최고 관리자(admin)** - `admin` 계정은 모든 역할/관리자 판정(프로젝트
  owner/editor/viewer, 팀장, 그룹 관리자, 세부 접근 권한)과 API 키
  스코프 제한을 전부 우회한다. 코멘트 수정·개인 폴더 관리·git 자격증명
  삭제처럼 "본인 소유물만" 다루게 설계된 기능도 admin은 타인 것까지
  대행할 수 있다.
- 설계자 프로필 - 본인 email/phone 등록 + 타인 공개 여부 선택, 최근
  작업 이력 조회(숨겨진 프로젝트의 활동은 멤버/팀장만 봄).
- 닉네임 + `#N` 자동 넘버링 - 중복 닉네임을 같은 풀 안 순번으로 구분
  (7일 변경 쿨다운), 미설정 시 공통 라벨 "설계자"도 같은 방식으로 번호가
  붙는다. 코멘트/메시지/질의/리비전/멤버 목록 등 사용자 참조가 전부
  `[닉네임 #N]`(`[userId]` 태그, 클릭 시 프로필로 이동) 형식으로 표시.
- git 자격증명 저장(AES-256-GCM 암호화, 외부 저장소 인증용, 재사용 가능).
- **admin 대행 비밀번호 재설정** - 이 시스템엔 이메일 발송 인프라가
  없어 self-service 찾기/재설정 대신, 웹 UI "사용자 관리" 화면(또는
  `docs user reset-password`)에서 admin이 다른 설계자를 골라 임시
  비밀번호를 1회 노출로 발급한다(API 키 secret과 동일한 노출 패턴).
  admin 여부는 `GET /api/auth/me`의 `isSuperAdmin` 필드로 프런트에
  노출되지만, 실제 접근 제어는 항상 서버(`requireSuperAdmin`)가 한다.
- **본인 비밀번호 변경** - 위 admin 대행 재설정과는 별개로, 이미
  로그인돼 있고 현재 비밀번호를 아는 통상적인 경우엔 웹 UI "내 정보"
  화면(또는 `docs auth change-password`)에서 현재 비밀번호 확인 후
  바로 새 비밀번호로 바꾼다 - 이메일 인증 없이 본인 인증(현재 비밀번호)
  만으로 처리되는 self-service 경로. CLI 전용(MCP엔 없음) - `auth
  register/login/logout`과 같은 급의 신원 관리 동작이라 비밀번호가
  대화 컨텍스트에 남지 않도록 의도적으로 뺐다.
- **사용자 관리 화면(admin 전용)** - 전체 설계자 목록을 아이디/닉네임/
  이메일로 검색 + 페이지네이션. 사용자명을 누르면 해당 프로필
  화면(`/users/:id`)으로 이동. "소속 조회" 버튼을 누르면 그 사용자가
  실제로 속한 프로젝트 그룹/팀/프로젝트를 한 다이얼로그에서 확인하고,
  각 소속에서 강제 방출할 수 있다(단, 그 프로젝트의 유일한 owner나
  그 팀/그룹의 유일한 관리자는 방출 불가 - 다만 그룹의 부모 팀에 다른
  팀 관리자가 있으면 그 팀 관리자가 상속으로 계속 그룹을 관리하므로
  예외적으로 허용). 기존 "접근 제한 보기"(문서/문서타입별 세부 권한
  제한 조회)와는 별개 버튼 - 둘은 서로 다른 개념(소속 여부 vs 세부
  제한)이라 함께 둔다.

## 2. API 키 (신원 위임 인증, 3종)

- 로그인 없이 REST API/CLI/MCP/웹 UI 전반을 쓸 수 있는 `cnwk_...` 키 -
  전부 "그 키를 만든 설계자의 신원을 그대로 대행"하되 접근 범위가
  다르다.
  - **개인 키** - 로그인과 동등, 접근 가능한 모든 프로젝트.
  - **프로젝트 개인 키** - 그 프로젝트 멤버 누구나 발급, 그 프로젝트
    하나로 한정.
  - **팀 관리 키** - 팀장 전용, 그 팀 소속 모든 프로젝트로 한정.
- 스코프 제한은 라우트 개별이 아니라 핵심 권한 판정 함수 안에서
  `AsyncLocalStorage`로 요청 전체에 자동 전파(라우트 수십 개를 안
  건드려도 전부 스코프 제한을 받음).
- 범위가 있는 키로는 신원 자체를 다루는 동작(프로필 수정, git
  자격증명, 팀/그룹/프로젝트 생성, 키 관리 자체)을 할 수 없다(권한
  상승 방지).
- **최고 관리자(admin) 소유 키는 스코프 자체를 완전히 무시**한다 -
  admin이 발급한 팀/프로젝트 단위 키도 그 스코프 밖의 임의의
  프로젝트에 접근 가능(일반 설계자의 동일한 모양의 키는 여전히
  스코프 제한을 받음).
- 키 값(secret)은 생성 시 응답에 딱 한 번만 노출, DB엔 단방향 해시만
  저장. 배제(soft revoke)는 행을 남기고 상태만 바꿔 감사 기록을 보존.
- **선택적 만료(TTL)** - 생성 시 만료 시각을 지정할 수 있다(미지정이면
  배제 전까지 무기한, 기존 동작과 동일). 지난 키는 인증에서 배제된
  키와 동일하게 거부되고, 목록에는 `active`/`revoked`와 구분되는
  `expired` 상태로 표시된다(저장이 아니라 조회 시점에 파생).
- 웹 UI: 프로젝트 "키 관리" 탭, 팀 화면 "키 관리" 토글에서 각각 관리.
  **내 정보 화면 "API 키" 카드는 탭 2개로 나뉜다**(`#profile-api-keys-
  tabs`) - "개인 키" 탭(기존과 동일, 생성/배제)과 "프로젝트 키" 탭
  (여러 프로젝트에 걸쳐 만들어둔 프로젝트 키를 한 곳에 모아 조회/배제
  전용으로 보여줌 - 새 프로젝트 키는 여전히 그 프로젝트의 "키 관리"
  화면에서 만든다). 키 관리(`docs key create/list/revoke`)는 CLI 전용,
  MCP엔 없음(신원 관리 동작이라 완전성 원칙의 의도적 예외).

## 3. 조직 구조 - 팀 / 프로젝트 그룹 / 프로젝트

- **계층**: 팀(선택적) → 프로젝트 그룹 → 프로젝트. 팀 없이 그룹만
  써도 되고, `InstallConfig.teamsEnabled`로 설치 전체 팀 기능을 끌
  수도 있다.
- **CRUD 전체** - 팀/프로젝트 그룹 둘 다 생성/이름 수정(팀은 `enabled`
  토글도)/삭제(비어있지 않으면 명확한 에러로 거부 - 고아 방지). 만든
  사람이 자동으로 첫 관리자(`TeamAdmin`/`ProjectGroupAdmin`)로
  등록된다.
- **프로젝트 그룹 재소속** - 그룹을 다른 팀으로 옮기거나 팀 없음으로
  뗄 수 있다(산하 프로젝트는 그룹의 `projectGroupId`만 참조하므로
  그룹 하나만 옮기면 자동으로 함께 이동). 그룹을 떼어내거나 이름만
  바꿀 땐 그 그룹의 관리자 권한만 있으면 되지만, 팀이 있는 다른
  팀으로 옮길 땐 **목적지 팀의 팀장 권한도 추가로 필요**(관리자
  상속이 조용히 바뀌는 걸 막기 위해 - 아래 항목 참고). `admin`은
  둘 다 우회.
- **관리자 상속** - 팀은 그룹의 상위 개념이라, 팀장은 자기 팀 산하
  모든 그룹에 대해서도 자동으로 그룹 관리자 권한을 갖는다(별도 등록
  행 없이).
- **팀/그룹/프로젝트 기본 비공개 가시성** - 권한이 없거나 소속되지
  않은 팀/프로젝트 그룹/프로젝트는 목록/조회 어디에도 나타나지 않는다
  (생성 시 기본값 `isPublic=false`). 각자 독립적인 "공개" 설정을
  가진다:
  - **팀/그룹**: `isPublic=true`거나, 그 팀/그룹의 관리자(admin
    포함)거나, 산하 프로젝트 중 하나라도 실제 멤버(`Member`)면 보인다
    (그룹 관리자는 소속 팀장 상속을 포함).
  - **프로젝트**: 멤버·소속 팀 팀장·소속 그룹 관리자는 `hidden` 여부와
    무관하게 항상 본다. 그 외엔 `hidden`이면 무조건 안 보이고(공개
    여부와 무관 - `hidden`이 `isPublic`보다 우선), `hidden`이 아니면
    `isPublic=true`이면서 그 그룹에 실제 멤버십이 있을 때만 보인다
    (단순 공개만으론 부족 - "그 그룹에 대한 읽기 권한"도 필요, 새
    모델 없이 기존 멤버십으로 판단). 목록에 보이면 프로젝트 개요(단건
    조회)도 열람 가능 - 단, 이 가시성이 문서/코멘트/칸반 등 실제
    콘텐츠 열람 권한(`requireProjectRole("viewer")`)까지 자동으로
    주는 건 아니다.
  - 숨김/공개 토글은 owner, 소속 팀 팀장, 소속 그룹 관리자 전부 가능
    (통일된 권한 집합).
- **계층별 멤버 가시성(보안)** - 프로젝트 멤버는 그 프로젝트 멤버끼리만,
  프로젝트 그룹 산하 전체 프로젝트의 멤버 합은 그 그룹 관리자만, 팀
  산하 전체는 그 팀 관리자만 볼 수 있다(그 외 요청은 403).
- 문서 타입을 팀/프로젝트 그룹 단위로 획일화해 정하는 기능은
  **의도적으로 없음** - 문서 타입은 항상 프로젝트 자신에게만 정의된다
  (5절 참고). `TemplateFile`(CLAUDE.md/SKILL.md 템플릿의 팀/그룹/
  프로젝트 override, 16절)은 완전히 별개 기능이라 이 원칙과 무관.

## 4. 멤버십 & 세부 접근 권한

- **프로젝트 역할 3단계**: owner(전권) > editor(읽기/쓰기) >
  viewer(읽기만). 프로젝트 생성자는 자동으로 owner.
- owner는 자기 자신의 owner 권한을 스스로 해제할 수 없다(다른 owner가
  내리는 것은 허용 - 프로젝트가 관리자 없이 잠기는 사고 방지).
- **문서/문서타입/프로젝트 3단계 세부 접근 권한 오버라이드** - owner가
  협업 설계자의 읽기/쓰기/삭제 권한을 문서별 > 문서타입별 > 프로젝트
  공통 순(더 구체적인 쪽이 우선)으로 제한하거나 예외적으로 넓힐 수
  있다. 제한이 적용되면 CLI/MCP 응답에 `notices` 배너가 붙어 AI가
  놓치지 않게 안내한다. owner 자신을 겨냥한 오버라이드는 무시(자기
  잠금 방지).
- API 키 스코프가 오버라이드보다 우선(비멤버 오버라이드로도 스코프
  밖 프로젝트는 못 들어감).
- **설계자별 접근 제한 프로젝트 횡단 조회** - "이 설계자가 전체
  설치에서 어떤 세부 접근 제한을 받고 있는지"를 프로젝트를 가로질러
  한 번에 볼 수 있다. 본인은 스스로(CLI `access-overview`/MCP
  `access_overview`/웹 프로필), 관리자는 임의의 설계자를 지목해서
  (CLI `user access-overview <userId>`, 웹 사용자 관리 화면의 "접근
  제한 보기") 조회할 수 있다 - 평소 그 프로젝트를 볼 수 있는지와
  무관하게 프로젝트 이름까지 항상 노출한다.

## 5. 문서 타입 / 상태 체계

- **DocType**(문서 분류) - 관리자가 프로젝트 안에서 자유롭게 정의
  (코드 영문 2글자 + 라벨 + 자연어 지침). 새 프로젝트는 기본 6종으로
  시작: `SP`(설계 명세)/`DC`(결정 요구사항 및 요청)/`PL`(실행 계획)/
  `PD`(실행 결과 보고)/`RM`(지시 사항/지침)/`DS`(설계 결정).
  **`QU`/`KB`는 시스템이 예약해 못 쓴다** - 질의(Question)/칸반 카드
  추적코드가 이미 이 접두어를 쓰고 있어, DocType이 같은 코드를 쓰면
  추적코드만으로 문서인지 질의인지 구분이 안 된다(생성/이름 수정 둘
  다 거부, 대소문자 무관).
  **기본 6종은 이름(code/label)을 바꿀 수 없고 삭제만 가능**하다 -
  관리자가 직접 추가한 타입은 이름도 자유롭게 고칠 수 있다(지침은
  기본/커스텀 구분 없이 항상 수정 가능). 어느 쪽이든 **그 타입으로
  만든 문서가 하나라도 남아있으면 삭제가 거부**된다(상태/세부 권한
  오버라이드는 삭제 시 함께 정리됨). 추적 코드 접두어는 문서
  생성 시점의 타입 코드로 영구 고정되므로, 이름을 나중에 바꿔도
  기존 문서의 추적 코드는 그대로 남는다.
- **DocStatus**(문서의 현재 상태) - 자유 정의가 아니라 표준 6개 코드로
  고정: `draft`/`review`/`pending`/`approved`/`deprecated`/`archived`.
  **`draft`가 아닌 상태에서 `draft`로는 어떤 경우에도 되돌아갈 수
  없다** - 그 외의 어떤 상태 조합으로 옮길 수 있는지는 설계자가 미리
  그래프로 규제하지 않는다(상태 전이를 정의/제한하는 CRUD 자체가
  없음) - 실질적인 작업자인 AI가 그때그때 판단한다(설계자 지시).
  **어떤 DocType이든 만들어지는 순간 이 표준 6개 상태를 항상 자동으로
  전부 갖는다** - 개별로 상태 코드를 골라 붙이거나 별도로 "표준 흐름"을
  적용하는 절차 자체가 없다(설계자 지시).
- **자연어 지침(guideline)** - "이 타입/상태는 무엇을 위한 것인지"를
  타입/상태 단위로 적어두면 문서 생성·상태 전이 화면에 힌트로 뜬다.
- 팀/그룹 단위 상속은 없음(3절 참고) - DocType은 항상 정확히 하나의
  프로젝트에만 속한다.

## 6. 문서 CRUD + 버전 이력 + 링크 + 추적 코드

- 문서 생성/조회/본문 저장/상태 전이/삭제(삭제는 리비전·링크·질의/
  답변까지 함께 정리).
- **생성/저장/전이/우선순위/보고서 생성 응답에는 본문이 없다** -
  `docs new`/`save`/`transition`/`priority-set`/`report-new`(및 대응
  MCP 도구 `document_new`/`document_save`/`document_transition`/
  `document_priority_set`/`report_new`)는 전부 본문(`body`)이 빠진
  가벼운 결과만 돌려주고, 대신 `updatedAt`(epoch ms, Prisma가 매
  갱신마다 자동 갱신)을 변경 여부 확인용 캐시키처럼 노출한다 - 호출자가
  이미 그 본문을 알고 있는 호출(생성/저장/보고서 생성 - 방금 자기가
  보낸 내용)이거나 본문을 안 건드리는 호출(전이/우선순위 설정)인데도
  매번 전문을 그대로 돌려주던 낭비를 없앤 것(`#document-list-
  lightweight`와 같은 문제 - 특히 MCP로 호출하는 AI 세션에서는 상태
  하나 바꿀 때마다 전체 본문이 도구 응답에 실려 컨텍스트를 잡아먹는
  실질적 비용이었음). 본문이 필요하면 `get`/`read`/`grep`(MCP도
  동일)으로 이어서 조회한다. `docs transition-bulk`/
  `document_transition_bulk`는 원래도 본문이 없던 항목별 결과라 대상
  아님.
- **본문 정규식 검색**(`docs grep <trackingCode> <pattern>`/
  `document_grep`, `#posix-regex-parser`) - 정규식 규격은 **POSIX
  ERE**로 고정돼 있다(`grep -E`/`egrep`과 같은 문법, `core/
  posixRegex.ts`가 실제로 패턴을 문자 단위로 파싱해 동등한 JS
  RegExp로 번역). `[[:alpha:]]` 같은 POSIX 문자 클래스는 지원되고,
  반대로 `\d`/`\w`/`\s`/`\b` 같은 JS 전용 단축 클래스는 POSIX 문법이
  아니라서 그 알파벳 리터럴로 취급된다. 대괄호 미종료·알 수 없는
  POSIX 클래스처럼 패턴 자체가 잘못됐으면 조용히 다른 걸로 둔갑하지
  않고 명확한 에러를 반환한다(예전엔 JS 정규식 시도 실패 시 리터럴
  문자열 검색으로 조용히 폴백했음 - 폐기). 소스 코드 파일 검색
  (`docs git grep`, §13)도 같은 파서를 공유해 규격이 동일하다.
- **일괄 상태 전이**(웹에는 전용 UI가 없고 CLI/MCP로만 가능) - 여러
  문서를 한 번에 같은 상태로 전이한다. 선택한 문서들이 서로 다른
  프로젝트/타입/권한을 가질 수 있어 전부-성공/전부-실패가 아니라
  항목별 결과를 반환한다(일부가 막혀도 나머지는 계속 진행). **일괄
  폴더 이동**(`PUT /api/documents/bulk-folder`, 웹 전용 REST - 폴더는
  AI가 모르는 개념이라 CLI/MCP엔 없음)도 같은 항목별 결과 방식이지만
  웹에 전용 UI가 없다 - 폴더 배정은 문서 읽기 페이지의 "폴더"
  다이얼로그로 한 건씩 처리한다(엔드포인트 자체는 남아있어 직접
  호출은 가능, §7 참고).
- **챕터(헤딩 섹션) CRUD**(CLI `docs chapter list|get|set|add|delete`/
  MCP `chapter_list|get|set|add|delete`, `#document-chapters`) - 긴
  문서를 매번 전체 본문으로 안 읽고/안 덮어써도 되게, 본문을 마크다운
  헤딩(`#`~`######`) 단위 "챕터"로 나눠 개별 조회·교체·삽입·삭제한다.
  챕터 하나는 그 헤딩 + 더 깊은 하위 헤딩들의 내용까지 포함(같은
  레벨 이하의 다음 헤딩 전까지). 번호(ordinal)는 제목 텍스트가 아니라
  매번 본문에서 새로 계산되는 1-based 순번(중복 제목이 있어도 항상
  명확). 첫 헤딩 앞에 내용이 있으면 0번("서문")으로, 헤딩이 아예
  없는 문서는 전체를 가리키는 챕터 1개로 취급한다. 쓰기는 내부적으로
  기존 `saveDocumentBody`를 그대로 거쳐 리비전·검색 재동기화가 유지된다
  (별도 쓰기 경로 없음). 스키마 변경 없음 - 매번 본문에서 즉석 계산.
  **웹 에디터에도 "챕터" 탭이 있다**(문서 편집 화면의 "보기"/"질의·답변"
  탭 옆) - 챕터 목록을 레벨별로 들여써서 보여주고, 챕터마다 편집/삭제
  버튼, "+ 챕터 추가" 폼(위치: 문서 맨 앞/맨 끝/특정 챕터 번호 앞·뒤)이
  있다. 한 번에 편집/추가 폼 하나만 열 수 있다(아코디언 - 여러 개를
  동시에 열면 저장 순서에 따라 다른 편집 중인 내용의 줄 범위가 밀려
  꼬일 수 있어서). 개별 챕터 편집·추가는 전체 본문 편집과 같은
  Monaco 에디터를 재사용한다. 챕터를 쓰면(교체/삽입/삭제) "보기"/
  "편집" 탭이 보는 본문도 자동으로 새로고침돼 항상 같은 내용을 본다.
- **본문 부분 치환**(`docs patch`/`document_patch`, `#document-patch`) -
  본문 전체를 다시 구성해 보내지 않고 `oldStr`/`newStr`만 주는
  str_replace 방식. `oldStr`이 본문에 정확히 한 번만 있을 때만
  적용하고, 없거나 여러 번 있으면 아무것도 안 바꾸고 실패(`replaceAll`
  플래그로 일치하는 곳 전부 교체 가능). 내부적으로 `saveDocumentBody`
  를 그대로 거쳐 리비전·검색 재동기화 유지. `docs patch-batch <file>`/
  `document_patch_batch`(로컬 JSON 파일 또는 items 배열)로 서로 다른
  문서에 각자 다른 치환을 한 번에 적용할 수도 있다(항목별 성공/실패
  반환).
- **버전 이력**(`DocumentRevision`) - 저장할 때마다 리비전이 쌓이고
  diff로 비교 가능.
- **문서 간 링크**(`DocumentLink`, `#document-link-ordering`) -
  역참조(backlinks) 조회에 더해, 정방향 링크 조회(`docs links-out`/
  `document_links_out` - 이 문서가 링크한 문서들을 순서대로)·삭제
  (`docs unlink`/`document_unlink`)·순서 변경(`docs links-reorder`/
  `document_links_reorder`)까지 지원한다. 새 링크는 항상 그 문서의
  현재 링크 목록 맨 끝에 자동으로 붙는다(순서 필드는 처음엔 링크를
  추가한 순서 그대로) - 여러 문서를 순서 있는 챕터로 엮은 구조(보고서/
  매뉴얼, §5 "보고서 생성" 참고)를 실제로 재배치·정리할 수 있게 하는
  게 목적. **문서 단건 조회(`docs get`/`document_get`) 응답에도
  `linksOut`/`backlinks`가 함께 온다**(`#document-detail-related-codes`) -
  본문만 봐서는 알 수 없는 "이 문서가 다른 문서와 어떻게 연결돼
  있는지"를 별도 호출 없이 바로 확인할 수 있다. **웹 UI "문서간 관계"
  서브탭**(문서 탭의 "답변 기록" 오른쪽, `#document-link-graph`,
  가로 폭 제한 없이 전체 폭 사용) - 프로젝트의 `DocumentLink` 전체를
  그래프로 보여준다(정방향/역방향 둘 다, 링크가 하나도 없는 고립
  문서는 표시 안 함). "관계도"(§22, `CodeRelation` - 코드/파일이 대상)
  화면이 이미 쓰는 Cytoscape 캔버스/네트워크형·계층형 뷰 모드 전환을
  그대로 재사용하되, 완전히 별개의 데이터(문서↔문서 링크 vs 코드→문서
  참조)라 서로 다른 화면 - 이름이 비슷해 보여도 혼동 금지. 노드를
  클릭하면 우측 상세 패널에 그 문서 정보(추적코드/상태/제목/분류/
  우선순위/수정 시각 + linksOut/backlinks 칩 목록, **본문은 제외**)가
  뜬다(`#document-link-graph-detail-panel`) - 패널의 관련 문서 칩을
  클릭하면 그래프 안에서 그 노드로 선택이 옮겨가며 계속 탐색할 수
  있고, 실제 편집 화면으로 넘어가려면 패널의 "전체 화면에서 열기"를
  쓴다. CLI `docs doc-graph <projectId>`/MCP `document_graph`로 같은
  그래프 데이터를 JSON으로도 받을 수 있다. **문서 편집/읽기
  화면에도 "연관 문서" 섹션**이 있다(`#document-editor-related-
  docs`, "연관된 소스 코드" 바로 위) - 그래프 탭을 열지 않고도 이
  문서가 링크한 문서/이 문서를 링크한 문서 목록을 바로 보고 클릭해
  이동할 수 있다(조회 전용 - 링크 추가/해제는 그래프 탭이나
  CLI/MCP `docs link`/`docs unlink`로).
- **추적 코드 중앙 레지스트리**(`TrackingCode`) - `XX-XXXXXXXX`(영문
  2글자 + hex 8글자) 형식 코드를 원자적 insert로 발급 예약(엔티티별
  `@unique`만으로는 못 막는 타입 간 충돌 방지). 유일성은 프로젝트
  단위(팀/프로젝트 경계를 넘지 않음 - 서로 다른 프로젝트는 같은 코드
  문자열을 독립적으로 가질 수 있음).
- **연관 소스 코드 연결** - 문서에 git 파일 경로를 직접 연결해두고
  클릭하면 소스 브라우저가 그 파일을 바로 열어 보여준다(연결/해제/
  목록 조회 - AI가 쓰는 신호라 CLI/MCP에도 노출).
- **연관 브랜치 연결** - 문서에 git 브랜치명을 연결해 "이 문서가 어느
  브랜치들에서 구현·논의됐는지" 식별한다(연결/해제/목록 조회, CLI/MCP
  노출 - 연관 소스 코드 연결과 완전히 같은 패턴). **그 브랜치가 나중에
  Gitea에서 삭제돼도 이 연결은 지워지지 않는다** - 코드 관계도의
  브랜치 스코프(§22, 브랜치 삭제 시 즉시 일괄 삭제)와 반대로, "이 문서가
  어떤 브랜치들을 거쳐 구현됐는지"는 브랜치가 사라진 뒤에도 역사적
  기록으로서 가치가 있다고 보아 영구 보존하는 의도적 비대칭.
- **보고서 생성** - 별도 문서 타입으로 다룰 수 있는 보고서 작성 경로.
- **우선순위(정수)** - 문서 상태가 `review` 또는 `pending`일 때만
  설정/갱신 가능(그 외 상태에서는 거부, 벗어나도 기존 값을 자동으로
  지우진 않음). 웹 UI/CLI(`docs priority-set`)/MCP
  (`document_priority_set`) 전부에서 조회·설정 가능.

## 7. 문서 정리 폴더 (AI 비가시, 설계자 개인 소유)

- DB에만 존재하는 순수 정리용 계층(실제 git 파일 트리와 무관) - 문서를
  폴더에 넣거나 뺄 수 있다(임의 깊이 중첩 폴더 지원).
- **"문서" 탭은 폴더 / 문서 분류 / 리스트 서브탭으로 나뉜다**
  (`#documents-tab-redesign`, 이후 "답변 대기"/"답변 기록" 서브탭이
  같은 자리에 추가됨 - `#document-answer-status-subtabs`, 8절 참고) -
  목록은 전부 페이지네이션되고(20건씩, 이전/다음), 목록의 문서 행을
  클릭하면 바로 그 문서 읽기 페이지로 이동한다:
  - **폴더**: 좌측에 폴더 트리, 우측에 선택된 대상의 문서 목록. 트리
    맨 위에 고정 항목 두 개 - "전체 문서"(폴더 선택 해제 상태, 전체
    문서 표시)와 "미분류 문서"(가상 폴더, 어느 폴더에도 안 담긴
    문서만) - 그 아래 실제 폴더 트리가 이어진다. 폴더를 누르면
    "선택"만 되고(우측 목록이 그 폴더의 문서로 갈림), 캐럿(▸/▾)은
    하위 폴더 펼침/접힘만 담당한다 - 문서 자체는 트리 안에 없다.
  - **문서 분류**: 좌측에 "전체 분류" + 문서 유형(SP/DC/...) 목록,
    우측에 선택된 유형의 문서 목록.
  - **리스트**: 폴더/분류 구분 없이 프로젝트 전체 문서 목록, 우측
    상단 정렬 콤보박스(최신순/최근 수정순/오래된 순)로 순서를 바꿀
    수 있다.
- **드래그로 재배치되는 건 폴더뿐** - 폴더를 다른 폴더 밑으로 끌어다
  옮기거나 같은 부모 안에서 순서만 바꿀 수 있다(자기 자신의 하위
  폴더 위로는 못 옮김 - 순환 방지, 마우스/터치 둘 다 지원). **문서를
  드래그로 폴더에 옮기는 기능은 없다** - 문서 목록과 폴더 트리가 서로
  다른 패널로 분리돼 있어, 문서를 다른 폴더로 옮기는 경로는 아래
  "문서 읽기 페이지의 폴더 버튼"뿐이다.
- 폴더 트리 안에서 어느 깊이에서든 "+" 버튼으로 그 자리에 하위 폴더를
  만들 수 있다.
- **삭제**: 비어있으면(하위 폴더/문서 배치 없음) 바로 삭제된다. 비어
  있지 않으면 두 옵션 중 하나를 선택해야 한다 - **재귀 삭제**(하위
  폴더와 그 안의 문서 배치까지 전부 삭제, 문서 자신은 안 지워짐) 또는
  **상위로 끌어올리기**(직속 하위 폴더/문서 배치를 부모 폴더로 한
  단계 옮긴 뒤 빈 폴더가 된 대상만 삭제 - 최상위 폴더면 하위 폴더는
  새 최상위가 되고 직속 문서 배치는 "폴더 없음" 상태가 됨). **어느
  경우든 문서 자신은 절대 삭제되지 않는다** - 폴더 삭제는 항상 정리
  메타데이터(배치 정보)만 지운다.
- **문서 읽기 페이지의 "폴더" 버튼**(코멘트 버튼 왼쪽) - 누르면 순수
  폴더 트리(문서 없음, 읽기 전용) 다이얼로그가 뜨고, 폴더 하나를
  골라 "이동"을 누르면 그 문서가 그 폴더로 옮겨진다 - 문서를 폴더에
  배정/재배정하는 유일한 경로.
- **만든 설계자 한 명만의 것** - 같은 문서를 여러 설계자가 각자 다른
  폴더에(또는 아무 폴더에도 안) 넣을 수 있고, 다른 사람의 폴더는
  존재 자체가 보이지 않는다. 프로젝트 멤버면 viewer도 자기 폴더를
  만들고 쓸 수 있다. **공유/권한 제한 기능은 의도적으로 없음** -
  폴더는 처음부터 끝까지 개인화 기능이라는 게 설계 원칙(재검토 후
  재확정 - `#folder-access-ui`).
- **AI(CLI/MCP)는 이 개념 자체를 모른다** - 관련 명령/도구/응답
  필드가 전혀 없음(완전성 원칙의 의도적 예외 - 웹 UI 전용).
- **문서 즐겨찾기**(`#document-favorites`) - 폴더와 같은 설계자 개인
  소유 패턴((문서, 설계자)당 최대 한 행, 계층 없는 단순 토글)이지만
  별개의 독립 기능. 문서 읽기 페이지의 "폴더" 버튼 왼쪽에 별(☆/★)
  버튼이 있어 누르면 즉시 즐겨찾기 등록/해제된다. 프로젝트 홈에
  "즐겨찾기한 문서" 섹션(최근 5건, "최근 변경 문서"와 같은 자리
  패턴)이 뜨고, "더보기"를 누르면 `/documents?favorites=1` 전용
  페이지에서 프로젝트 전체 즐겨찾기 문서를 페이지네이션으로 훑을 수
  있다. AI(CLI/MCP)는 이 개념도 모른다(웹 전용).

## 8. 질의/응답 (Q&A) - AI가 묻고 설계자가 답한다

- 대상은 문서/칸반 카드/소스 코드 파일 세 가지(`targetType`/
  `targetKey`로 다형화 - 대상이 늘어도 명령 시그니처는 안 바뀜).
- **질의는 AI가 등록, 설계자는 답변만** - `kind`로 `answer`(자유
  텍스트 답변)/`approval`(승인·거부만, 텍스트 대신 `--decision`)
  구분.
- 상태: `open`(질의 등록, 답변 대기) → `pending`(설계자 답변 완료,
  AI 확인 대기 - 종결 아님) → `resolved`(AI가 확인 완료 표시).
  확인 완료 표시는 CLI `question-ack-bulk`/MCP `question_ack_bulk`로
  여러 건을 한 번에 처리할 수도 있다(항목별 결과 반환, 마이그레이션
  직후처럼 pending이 몰려 있을 때 - 웹 UI엔 없음, CLI/MCP 전용).
  **`withdrawn`**(질의를 낸 본인 또는 관리자가 철회) - 더 이상
  유효하지 않게 된 질문을 정리한다. `open` 상태일 때만 철회 가능
  (이미 답변된 질문은 되돌리지 않고 그대로 `ack`로 마무리).
- 판단에 참고한 문서를 `--refs`로 구조적으로 태깅(`QuestionReference`).
- **AI가 선택지를 제안 가능**(각 옵션에 라벨 + 부가 설명) - 클릭하면
  답변 입력칸에 채워짐.
- 답변으로 문서 상태가 자동 전이될 수 있다(그 문서 타입에 유일하게
  허용된 다음 상태가 있을 경우 - 모호하면 자동 전이 안 함, 칸반 카드/
  소스 코드 대상은 이 개념 자체가 없음).
- 웹 UI(`QAPanel.vue`) - 문서 에디터의 [질의/응답] 탭, 소스 코드
  화면, 칸반 카드 다이얼로그에서 등록/답변/승인·거부 가능(CLI 없이도
  전체 워크플로우 가능).
- **질의 자신의 추적코드(`QU-XXXXXXXX`)를 문서 본문/코멘트/메시지/
  다른 질의 본문 등 어디서 클릭해도 전용 미리보기(`QuestionDialog.vue`)가
  뜬다** - 종류/상태/본문/참고 문서/제안 선택지/답변(승인·거부·본문)을
  읽기 전용으로 보여주고, "대상 열기"로 그 질의가 달린 문서/칸반
  카드로 바로 이동할 수 있다. (문서 본문의 마크다운 렌더링은
  `TrackingCodeText.vue`가 아니라 `MarkdownBody.vue`가 별도의 HTML
  후처리 정규식으로 추적코드 링크를 만드는 완전히 다른 경로라, 처음
  `QU-` 지원을 추가할 때 이쪽은 갱신을 놓쳐서 "문서 본문에서
  QU 코드를 누르면 Not Found"로 한 라운드 뒤에 따로 발견·수정됨 -
  `#notification-bell` 참고.)
- **"문서" 탭에 프로젝트 전체 질의 현황 서브탭 2개** - 폴더/문서
  분류/리스트 옆에 "답변 대기"(미답변 `open` + 설계자 답변완료·AI
  확인대기 `pending` - 기존 `/pending/page`를 그대로 재사용)/"답변
  기록"(AI 확인 완료까지 끝난 `resolved`만 - 신설 `GET /projects/:id/
  questions/resolved/page`)을 나란히 두어, 대상(문서/칸반 카드)을
  일일이 열지 않고도 프로젝트의 질의 상태를 한눈에 훑을 수 있다.
  각 행의 추적코드를 누르면 바로 위 `QuestionDialog`가 뜬다.
- **사이드바 알림 종 - 프로젝트 횡단 미확인 질의**(`#notification-bell`) -
  "내 정보" 왼쪽의 🔔 버튼이 내가 속한(`Member` 행이 있는) 모든
  프로젝트를 통틀어 "답변 대기"(open+pending) 질의 개수를 배지로
  보여준다(60초 주기 폴링 + 로그인 시 1회 조회, 99건 넘으면 "99+").
  누르면 최근 20건이 스크롤 가능한 드롭다운으로 뜨고(각 항목에
  프로젝트명 표시, 클릭하면 `QuestionDialog`), 패널 안 "더보기"를
  누르면 전용 페이지(`/notifications`)로 이동해 전체 목록을
  페이지네이션(`QuestionListPanel.vue` 재사용, 프로젝트명 칩 표시
  옵션 추가)으로 훑을 수 있다. 신설 `GET /auth/me/pending-questions/
  count`·`/auth/me/pending-questions/page`(`Member` 테이블로 내
  프로젝트 범위를 잡음 - `isSuperAdmin`의 암묵적 owner 승격은 안 씀,
  "내가 실제로 속한 프로젝트"만).

## 9. 코멘트 (설계자 전용 채널)

- 대상은 Q&A와 동일하게 문서/소스 코드 파일/칸반 카드 세 가지.
- open/closed/solved/etc 4단계 상태, 작성자 본인만 상태 변경 가능.
- 작성자 본인(또는 최고 관리자)만 수정/삭제 가능 - 그 외에는 읽기
  전용 배지만 보임.
- **설계자들끼리만 공유하는 채널로, AI의 참고 지표가 될 수 없다** -
  CLI/MCP 어디에도 없음(완전성 원칙의 의도적 예외, 웹 UI 전용).

## 10. 칸반 보드

- 프로젝트별 "분류(컬럼) → 카드" 구조로 진행 흐름을 추적.
- 분류는 설계자가 웹 UI에서 만든다(기본 pending/doing/qa/done 4개로
  시작). 순서/숨김은 **설계자 개인 설정**(다른 설계자의 화면엔 영향
  없음).
- 카드는 AI(CLI/MCP)와 설계자 양쪽 다 생성 가능 - 생성 시 판단 근거가
  된 문서를 `--refs`로 태깅. **설계자가 웹에서 만든 카드는 그 즉시
  메시지로 알림이 가고 "반드시 진행되어야 하는 작업"으로 표시**된다
  (기존 메시지 채널 재사용).
- 카드를 열면 근거 문서/받은 지시/코멘트를 다이얼로그로 확인. 카드
  코멘트는 문서 코멘트와 마찬가지로 CLI/MCP엔 노출 안 됨.
- 카드는 개별로 숨길 수 있다(전역 - 컬럼과 달리 보드 전체에 적용).
- 분류/카드 생성, 카드 숨김·이동은 editor 이상만 가능(웹 UI 버튼도
  권한 없으면 안 보임).
- **분류/카드 순서 변경 및 카드 이동은 `vuedraggable`(SortableJS)
  기반 드래그로 - 마우스/터치 양쪽 다 지원**(fallback 모드로 동작해
  네이티브 HTML5 드래그의 제약 없이 터치 기기에서도 동일하게
  작동). 카드 드래그는 터치에서만 살짝 눌러야 시작되도록 지연을 둬
  스크롤 제스처와 구분한다. 보드의 빈 배경을 마우스로 드래그하면
  좌우로 팬 스크롤(터치는 브라우저 기본 스크롤 그대로).
- **카드 상세 다이얼로그에서도 드롭다운으로 분류를 바로 바꿀 수
  있다** - 좁은 화면에서 드래그 없이도 카드를 다른 분류로 옮기는
  대안 경로(editor 이상만 노출).

## 11. 메시징 (대기/처리중/기록/장애 복구) + 실시간 전파

- 프로젝트 단위 세션 간 소통 - **대기/처리중/기록** 3단계로 분류한다.
  `deliveredAt`("AI가 CLI/MCP로 읽어갔는가")과는 별개 축으로,
  **`ackedAt`/`completedAt` 두 필드가 이 분류를 결정한다**: 대기
  (`ackedAt` 없음) → **명시적으로** `message ack`/`message_ack`를
  불러야 처리중(`ackedAt` 있음, `completedAt` 없음) → 다시
  **명시적으로** `message complete`/`message_complete`를 불러야
  기록(`completedAt` 있음, `ack` 없이 바로 불러도 `ackedAt`까지
  자동으로 채워짐). 단순히 목록을 조회하는 것만으로는 상태가 안
  바뀐다(설계자 지시 - "읽음"과 "처리 시작"을 분리). ack/complete는
  프로젝트 멤버 누구나 가능(보낸 사람이 아니어도 됨) - "누가 보냈나"가
  아니라 "누가 처리했나"를 기록하는 축이라서. 이미 처리중/기록인
  메시지에 다시 ack/complete를 불러도 에러 없이 그대로 반환
  (idempotent).
- **대기 상태(`ackedAt` 없음)인 메시지는 수정할 수 없다 - 삭제만
  가능**(설계자 지시). 처리중/기록으로 넘어가면 본인(또는 superAdmin)
  이 수정 가능.
- **CLI/MCP의 `message list`/`message_list`는 `--status`/`status`를
  생략하면 기본으로 `active`(대기+처리중)만 돌려주고 기록(완료)된
  메시지는 안 보낸다**(설계자 지시 - "ack된 것, ack 안된 것만 보내고,
  기록은 보낼 필요가 없다") - 이미 처리가 끝난 메시지를 AI 세션이
  매번 다시 받을 필요가 없다는 취지. 전체 이력이 필요하면
  `--status all`/`status:"all"`을 명시해야 한다. `pending`/`processing`/
  `delivered`(=기록) 개별 필터는 그대로 유지된다. 웹 UI(`메시지` 탭)는
  항상 명시적으로 탭에 맞는 status를 보내므로 이 기본값 변경의
  영향을 받지 않는다.
- 웹 UI(`메시지` 탭)는 대기/처리중/기록 3개 탭 - 대기 탭엔 "처리
  시작" 버튼만(수정 버튼 없음), 처리중 탭엔 "완료"+수정+삭제, 기록
  탭엔 수정+삭제만.
- **`message wait`는 CLI/MCP가 전용 MQTT 계정으로 EMQX를 직접
  구독해 대기한다** - 서버는 그 대기를 대신 떠맡지 않는다("CLI/MCP는
  REST만 호출" 원칙의 의도적 예외 하나, `cli/apiclient.ts`의
  `waitForMessageDirect()` 참고). `GET /api/auth/me/mqtt-credentials`
  로 본인 전용 자격증명(User당 1개, 최초 요청 시 지연 생성해 암호화
  보관)을 받아와 직접 접속·구독하므로, 원하는 만큼 오래 기다려도
  서버 커넥션을 붙들지 않는다(전체 대기 시간 상한은 여전히 1시간).
  `PUBLIC_EMQX_MQTT_URL`이 서버에 설정 안 돼 있거나 실제 연결이
  안 되면(방화벽 등) 예전 방식 - 서버가 최대 10초씩 짧게 구독해
  응답하는 HTTP 롱폴을 CLI/MCP가 반복 호출(폴링)하는 방식 - 으로
  자동 폴백한다(느리지만 계속 동작 - fail-soft).
- `message recent`는 상태를 바꾸지 않는 순수 조회 - 시스템 다운 후
  재접속 시 "마지막에 무슨 일이 있었는지" 확인용, 반복 호출해도 안전.
- 문서 에디터의 "메시지로 지시" 버튼은 그 문서의 추적 코드를 자동으로
  본문에 병기(editor 이상만 노출).
- 시스템(비인간) 발신 메시지도 지원(`authorId: null`) - 예: 외부
  저장소 동기화 충돌 시 자동 안내(13절 참고).
- **수정/삭제**(웹/CLI `message edit|delete`/MCP `message_edit`/
  `message_delete`) - 본인이 보낸 메시지만(또는 superAdmin) 가능,
  시스템 브로드캐스트는 일반 사용자가 못 건드림. 실시간 반영은
  `project/{id}/changes` topic으로 나가고(`message wait`가 구독하는
  `.../messages` topic과는 분리 - 그 토픽에 편집/삭제 이벤트를 올리면
  대기 중인 `message wait` 호출자가 "새 메시지"로 오인하게 됨).
- **EMQX 클라이언트 인증 + Member 기준 topic ACL** - `project/{id}/
  (changes|messages)` topic은 그 프로젝트 멤버만 구독/발행 가능.
  인증 방식은 셋: (1) 웹 UI는 설치 전역 JWT를 그대로 MQTT 인증에
  재사용해 MQTT-over-WebSocket으로 직접 소비(변경 추적 뷰 + 메시징
  패널이 새로고침 없이 갱신), (2) CLI/MCP의 `message wait`은 위
  전용 MQTT 계정으로 원본 MQTT(TCP)에 직접 접속, (3) 백엔드 자신의
  고정 서비스 계정(HTTP 폴백 경로 내부 연결).

## 12. 검색 / 인덱싱

- Meilisearch 기반 - 문서 목록/조회 등 대부분의 읽기가 DB가 아니라
  검색 엔진을 거친다(write-through - 쓰기 시점에 즉시 색인 동기화,
  Meilisearch SDK의 작업 완료까지 실제로 기다림).
- **사이드바 검색**(웹 전용) - 키워드 입력 후 범위 선택: (1) 이
  프로젝트 내, (2) 같은 프로젝트 그룹 안, (3) 같은 팀 안. "소스코드
  포함" 체크박스로 git 저장소의 소스 코드 내용까지 검색 대상에 포함
  가능(저장소 연결 시 백필, push마다 증분 동기화, 에디터 저장 시 즉시
  동기화). 결과는 항상 실제 멤버십으로 필터링.
- **Meilisearch 장애 대응**: Meilisearch에 연결할 수 없으면 조회
  요청은 즉시 503 + 명확한 한국어 메시지로 응답한다(내부 호스트명
  노출 없음, 문서 없음(404)과 혼동되지 않음). 장애 중 새 문서
  생성이나 소스 코드 동기화처럼 DB 쓰기가 이미 끝난 뒤 검색 인덱스
  반영만 실패한 경우는 그 반영 작업이 큐에 남아, 장애가 풀리면 30초
  주기 백그라운드 워커가 자동으로 일괄 재처리한다(admin이
  `search-queue status`/`search-queue drain`으로 큐 상태를 확인하고
  수동으로 즉시 비울 수도 있음). 기존 문서의 수정/삭제/전이도 장애
  중 막히지 않는다 - 그 라우트들의 사전 권한 확인은 검색 엔진이
  아니라 DB를 직접 읽고(`getDocumentAccessInfo`), 실제 색인 반영만
  큐로 넘어간다. 문서 **내용 조회**(`GET /api/documents/:trackingCode`
  등 진짜 "조회")만 여전히 검색 엔진을 거치므로 장애 중엔 503으로
  막힌다 - 조회는 큐로 나중에 대신할 수 없는 성격이라 의도된 경계.
- **대량 문서 목록 대응**: 문서 참조 선택기(Q&A 근거/칸반 카드
  근거/접근 권한 대상 문서 등 여러 화면이 공유하는 다이얼로그)와
  변경 추적의 "문서 버전 이력" 선택도 검색/페이지네이션 기반이라,
  프로젝트 문서 수가 많아도 전체 목록을 한 번에 안 받는다(검색어
  없으면 최근 문서 50개, 있으면 Meilisearch 전문검색으로 서버가
  직접 추림).
- **`docs search`/`document_search`의 응답 크기 축소 옵션** - 기본은
  히트마다 본문 전체를 담아 돌려주므로, `--lines <n>`(각 히트 본문을
  앞 n줄까지만 자름, 실제로 잘렸으면 `bodyTruncated:true`)이나
  `--codes-only`(본문/메타 없이 trackingCode 배열만 반환, 페이지네이션
  응답이면 `items`만 문자열 배열로 축소)로 필요한 만큼만 받을 수
  있다. CLI/MCP 둘 다 지원.

## 13. git 저장소 연동 + 동기화(제안/발행) + 브랜치/PR 관리

- **연결 방식 3가지**(웹 UI "설정" 탭 + CLI/MCP, **owner 전용** -
  admin은 예외):
  1. **새 저장소 생성** - Gitea에 빈 저장소를 만들어 연결.
  2. **외부 저장소 완전 이주** - 기존 저장소 히스토리를 통째로
     가져와 Gitea 저장소로 독립적으로 시작(1회성, 이후 원본과 무관).
  3. **외부 저장소 연동** - 외부 저장소가 계속 권위(authoritative)를
     갖고, Gitea에 미러(읽기 전용 pull 사본)와 작업 저장소(이
     시스템이 실제로 커밋하는 곳) 두 개를 만든다.
- **저장소는 프로젝트마다 별도 Gitea 조직(org) 네임스페이스 아래
  생성**(`GITEA_ORG_PREFIX`+projectId, 기본 접두어 `proj-` -
  #gitea-per-project-namespace) - 예전엔 설치 전체가 하나의 공유
  조직을 썼지만, 이제 프로젝트 하나당 org 하나(자체 호스팅은
  `repo`, 외부 연동은 `work`/`mirror` 두 저장소)로 분리된다. 협업자
  권한은 그대로 저장소별로 프로젝트 `Member.role`과 자동 동기화된다.
  self_hosted 저장소의 clone 주소(`repoUrl`)는 `PUBLIC_GITEA_URL`
  환경변수가 설정돼 있으면 조회 시점마다 그 값 기준으로 다시
  계산된다 - Gitea가 생성 시점에 돌려준 주소를 그대로 얼려두지
  않으므로, 도메인 이전(로컬→원격 이전 등) 후에도 이 값만 갱신하면
  즉시 반영된다.
- **보안 강화**(#gitea-nginx-lockdown) - Gitea는 호스트에 전혀
  노출되지 않는다(포트 매핑 없음). nginx 리버스 프록시가 유일한 기본
  노출 지점이고, `.git`로 끝나는 git smart-HTTP 요청만 Gitea로
  돌려주고 나머지(Gitea 자신의 웹 UI 포함)는 이 앱으로 보낸다 -
  즉 어떤 저장소도 특별한 명시(운영자가 임시로 포트를 여는 등) 없이는
  Gitea 자체 화면으로 조회될 수 없다. 최초 관리자 계정/PAT 발급도
  더 이상 웹 설치 마법사가 아니라 `docker exec ... gitea admin user
  create`/`generate-access-token` CLI로 한다(README.md "Gitea 설정"
  절). 기존 설치를 이 구조로 올릴 때는 `npm run migrate:gitea-namespaces`
  (+ `verify:gitea-namespaces`)로 기존 저장소를 실제로 새 네임스페이스로
  이전한다(멱등, 항목별 성공/실패 결과표).
- 비공개 저장소 인증 필요 시 그 자리에서 자격증명을 입력받아
  저장(재사용 가능)한 뒤 자동 재시도.
- **GitHub 로그인 + 저장소 선택**("외부 저장소 연동"/"외부 저장소로
  전환" 양쪽에서 "GitHub로 로그인" 버튼) - GitHub OAuth 팝업으로 승인
  하면 저장소 목록 다이얼로그가 뜨고, 고르기만 하면 URL/자격증명이
  자동으로 채워진다(URL 직접 입력 방식도 계속 가능). 서버 관리자가
  `GITHUB_OAUTH_CLIENT_ID`/`SECRET`을 설정하지 않았으면 버튼 자체가
  안 보인다(URL 직접 입력 방식은 그대로 동작).
- **자체 호스팅 → 외부 저장소로 전환**(owner 전용) - "새 저장소 생성"
  으로 시작한 프로젝트도 나중에 외부(GitHub/GitLab) 저장소를 권위로
  연동할 수 있다. 지금까지 쌓인 커밋 히스토리는 그대로 유지되고(작업
  저장소를 새로 만들지 않고 재사용), 전환 즉시 현재 상태를 외부
  저장소에 한 번 발행(push)한다.
- **GitHub OAuth 토큰 자동 갱신 + 자격증명 오류 시 자동 강등/파기**
  (`#credential-lifecycle`) - 외부 저장소로 발행(push)을 시도할 때마다
  먼저 저장된 자격증명이 아직 유효한지 확인한다. GitHub 조직의 "OAuth
  App 토큰 만료" 정책이 켜져 있어 access token에 유효기간이 있고
  refresh_token까지 함께 저장된 자격증명이면, 만료 5분 전부터 이
  확인보다 먼저 조용히 자동 갱신을 시도해 새 토큰으로 갈아끼운다 -
  보통은 이 단계에서 해결돼 아래 무효화 경로까지 가지 않는다(수동으로
  붙인 PAT처럼 refresh_token이 아예 없는 자격증명은 예전과 동일하게
  동작). 그래도(갱신 대상이 아니거나 갱신 자체가 실패해서) 토큰이
  정말 무효로 확인되면 자동으로 외부 연동이 해제되고 자체 호스팅으로
  전환되며(작업 저장소는 그대로 보존) - 이 무효 자격증명을 다른
  프로젝트도 함께 쓰고 있었다면 그 프로젝트들도 똑같이 자동
  전환되고, 저장돼 있던 자격증명 자체도(재사용 불가능하므로) 완전히
  삭제된다. 영향받은 프로젝트 각각에 안내 메시지가 남는다 - "충돌"과
  "자격증명 무효화"를 서로 다른 에러로 구분해 알려준다.
- **"동기화(발행)" 패널의 자격증명 만료 표시 + 일괄 확인** - 자격증명
  선택 목록에 GitHub 로그인(OAuth)으로 발급된 자격증명이면 access
  token 만료 시점을 함께 병기한다(`hostPattern (만료: 2026-03-05
  14:00:00)` 형식 - 만료 정책이 꺼져 있는 조직의 토큰이나 수동 PAT는
  만료가 없으므로 시점 없이 그대로 표시). "자격증명 일괄 확인" 버튼을
  누르면 이 사용자가 가진 자격증명 **전부**를 한 번에 검사한다(특정
  프로젝트 하나에 묶이지 않고 각 자격증명 자신의 hostPattern으로
  독립적으로 판정 - 서로 다른 프로젝트/호스트를 향하는 자격증명이
  섞여 있어도 정확하게 검사됨). 자동 갱신까지 실패한 진짜 무효
  자격증명은 그 자리에서 파기되고(그 자격증명을 쓰던 모든 프로젝트가
  함께 자동 강등), 결과 목록에 자격증명별 유효/무효 여부가 표시된다.
  hostPattern이 없어 판정 기준이 없는 자격증명은 건드리지 않고
  유효로 간주해 건너뛴다.
- **외부 저장소 연동 시 웹훅 자동 등록** - 성공하면(GitHub/GitLab
  API로 이 백엔드의 `PUBLIC_BACKEND_URL`을 등록) 그 저장소에 직접
  push해도(이 시스템을 안 거쳐도) push 훅 자동화가 반응한다. 이
  백엔드가 GitHub/GitLab이 실제로 도달 가능한 공개 주소가 아니면
  (로컬/사설 서버에 흔함) 자동 등록만 실패하고 다른 기능(연동/동기화
  확인/발행)은 전혀 안 끊긴다 - 웹 UI에 Payload URL/Secret이 담긴
  수동 등록 안내가 바로 뜬다(README.md "로컬/사설 서버에 설치한 경우"
  절 참고).
- **git log/diff/show/tree/file/blame 등 조회**(자체 호스팅·외부
  연동 둘 다, 작업 저장소 기준) - `git blame`만 Gitea REST API 자체
  한계로 미지원.
- **파일 저장/삭제**(CLI/MCP, 작업 저장소 기준) - `git put`(있으면
  갱신, 없으면 생성)과 그 대칭인 `git delete`(커밋으로 기록)를 둘 다
  지원한다. 없는 파일을 삭제하려는 시도는 명확한 에러로 실패하고,
  소스 검색 인덱스도 저장/삭제 양쪽에서 바로 갱신된다. 내부적으로
  Gitea의 "여러 파일을 한 번에 커밋" API(`POST /contents`)를 1개
  변경으로 호출하는 방식으로 통일돼 있다. CLI의 `git put`은 로컬
  파일을 읽어 그 내용을 그대로 전송하는데, Windows에서 CRLF로
  체크아웃된 파일을 실제 git commit과 동일하게 LF로 정규화해서
  보낸다(UTF-8 텍스트 전용 파이프라인 - 바이너리는 대상 아님).
- **소스 코드 정규식 검색**(`docs git grep <projectId> <path>
  <pattern>`/`git_grep`, `#posix-regex-parser`) - `docs grep`(문서
  본문)과 같은 파서(`core/posixRegex.ts`)를 공유해 정규식 규격이
  동일하게 **POSIX ERE**로 고정돼 있다(`grep -E`와 같은 문법,
  `[[:alpha:]]` 같은 POSIX 문자 클래스 지원, `\d`/`\w`/`\s` 같은 JS
  전용 단축 클래스는 그 알파벳 리터럴로 취급). 패턴 자체가 잘못됐으면
  에러를 반환한다(자세한 내용은 §6 참고).
- **git 조회/편집 캐시**(프로젝트 단위 DB 테이블, `GitTreeCache`/
  `GitBlobCache`) - 트리(`getFullTree`)와 파일 내용(`getFileContent`)
  조회가 매번 Gitea REST를 왕복하는 대신 이 캐시를 먼저 거친다. 이
  앱 자신의 write(저장/삭제/스테이징 커밋)는 그 자리에서 캐시를 즉시
  patch하고(1차, 지연 없음), `git my-token`으로 이 앱을 거치지 않고
  직접 push된 경우를 위한 Gitea 시스템 웹훅 기반 무효화도 걸려 있다
  (2차 안전망). 여러 파일을 한 번에 조회하는 `git cat-batch`/
  `git_cat_batch`도 이 캐시를 그대로 탄다. 블롭 캐시 쓰기는 내용
  자체의 해시(sha)가 키라 같은 내용이 동시에 두 번 캐싱을 시도해도
  (예: 커밋 직후 patch와 다른 조회 경로가 겹치는 경우) 조용히
  무시하고 넘어간다 - 캐시는 항상 최적화일 뿐이라, 캐시 갱신이
  실패해도 그 갱신을 유발한 실제 git 동작(커밋 등) 자체는 절대
  실패로 보이면 안 된다는 원칙.
- **스테이징(git add/rm/status/restore/commit)** - 실제 git처럼 여러
  파일 변경을 모았다가 한 번에 커밋할 수 있다. `git add`/`git rm`이
  스테이징만 하고(아직 Gitea에 반영 안 됨) - CLI의 `git add`도 `git
  put`과 같은 CRLF→LF 정규화를 거친다. `git status`가 스테이징된
  각 항목의 현재 HEAD 대비 diff(문서 버전 비교와 같은 형식)와 그 사이
  드리프트 여부를 보여주고, `git restore`가 스테이징을 취소한다.
  `git commit`이 스테이징된 변경을 모아 **한 번에 하나의 Gitea 커밋**
  으로 반영한다 - 드리프트(그 사이 다른 경로에서 같은 파일이 바뀜)가
  있어도 겹치지 않는 변경이면 3-way 자동 병합하고, 겹치는 진짜 충돌이
  하나라도 있으면 커밋 전체를 원자적으로 거부한다(부분 반영 없음 -
  스테이징은 그대로 남아 직접 병합 후 재시도 가능). "삭제 대 수정"
  조합은 항상 충돌로 취급한다(실제 git과 동일). git 경로는 항상
  `/` 구분자로 정규화된다(`#git-path-separator`) - Windows CLI에서
  `docs\X.md`처럼 백슬래시로 경로를 입력해도 슬래시 버전과 같은 키로
  취급된다(파일 조회/쓰기/blame/tree/raw, 소스 코드 연관 질의, 코드
  관계도 `filePath`도 전부 동일하게 정규화).
- **동기화 제안**(외부 연동 프로젝트 전용, 미리보기) - 미러 대비 작업
  저장소가 뭐가 달라졌는지 확인하고, 달라진 파일 내용을 미리 볼 수
  있다.
- **동기화(발행)** - "동기화" 버튼이 Gitea Push Mirror로 작업 저장소의
  커밋을 실제로 외부(권위) 저장소에 push한다. fast-forward 가능하고
  자격증명에 push 권한이 있으면 즉시 반영되고, 그렇지 않으면(충돌
  또는 권한 부족) AI 대기열(`GitSyncQueueEntry`)에 올라가며 그
  프로젝트에 시스템 메시지로 상황이 안내된다 - AI가 처리 후 완료를
  보고해야 버튼이 다시 활성화된다(자동 PR 생성은 범위 밖 - 항상
  검토를 거친 뒤 반영). 실패 경로는 두 가지를 전부 같은 방식으로
  처리한다(`#push-mirror-sync-trigger-error`) - Gitea의 push mirror
  동기화가 **비동기로**(트리거는 성공, 실제 push만 나중에 실패) 실패한
  경우와, 자격증명이 만료돼 인증이 **바로**(트리거 호출 자체가 동기적
  에러) 거부되는 경우 둘 다 항상 같은 대기열+안내 메시지로 이어진다 -
  후자를 구분 없이 처리 못해 raw Gitea 에러 메시지가 그대로 새던
  버그를 수정. 발행을 시도할 때마다 Gitea의 push mirror 등록을 항상
  현재 자격증명으로 삭제 후 재생성한다(`#stale-push-mirror-credentials`) -
  Gitea REST에 자격증명만 갱신하는 API가 없어, 안 그러면 재연동이나
  OAuth 토큰 자동 갱신으로 앱 DB의 토큰이 바뀌어도 Gitea 쪽은 최초
  등록 시점의 예전 토큰을 계속 써서 매번 같은 인증 실패로 발행이
  막히는 문제가 있었다.
- 소스 코드 파일 열람은 텍스트("보기"/"편집" 모드 분리, Monaco
  에디터)와 이미지/영상(브라우저 미리보기)으로 나뉘고, "원본 다운로드"
  버튼이 항상 있다.
- **외부 연동 해제**(owner 전용, CLI `git unlink`/MCP
  `git_unlink`/웹 UI "외부 연동 해제" 버튼) - 외부(GitHub/GitLab)를
  권위 저장소로 취급하던 관계만 끊는다. 미러 저장소는 삭제되고,
  지금까지 커밋해온 **작업 저장소는 그대로 유지되어 자체 호스팅
  저장소로 전환**된다(히스토리·현재 상태 전부 보존) - Gitea와의
  연결 자체는 프로젝트를 삭제하지 않는 한 끊을 수 없다(설계자 확정).
  **자체 호스팅 저장소는 해제할 수 없다** - 유일한 방법은 프로젝트
  삭제.
- **저장소 관리 탭**("저장소 관리" - 소스 코드 탭과 별개) - 브랜치
  목록(이름+마지막 커밋 시각+"탐색" 버튼, 다른 브랜치를 소스 코드
  화면에서 읽기 전용으로 열람 가능)과 Pull Request 개요:
  - **PR 생성**(editor 이상, 이 탭에서만) - head/base 브랜치 선택 +
    제목/설명, 설계자 본인의 Gitea 계정 명의로 생성된다.
  - 최신 5개 PR만 보여주고(제목/상태 배지 - 열림·닫힘·머지됨·거부됨)
    "더보기"를 누르면 전용 목록 페이지(페이지네이션, state 필터)로
    이동한다. 이 시스템에 브랜치 **생성** 기능은 없다 - 설계자가 자신의
    Gitea 자격증명으로 직접 push해 만든 브랜치가 목록에 나타난다.
  - 각 PR 항목을 누르면 **전용 상세 페이지**로 이동 - 헤더(제목/본문/
    상태+거부여부 배지/head→base), 4개 섹션(**메시지** - 이 앱이 머지/
    거부/닫힘/재오픈마다 남긴 진행 메시지, **커밋** - 관여한 커밋 목록,
    **대화** - Gitea PR 댓글(Markdown, editor 이상 작성 가능), **진행
    내역** - 댓글/상태전이/머지/커밋참조 등을 타입별로 구분한 전체
    타임라인 세로 피드), 액션 버튼(아래).
  - **머지**(**owner 전용** - editor는 PR을 만들 순 있어도 머지는 못
    한다, 버튼 자체가 owner가 아니면 안 보이고 서버도 403으로 거부).
  - **머지 실패 시 수동 완료 안내** - 자동 머지가 실패하면 그 사유가
    영구 보관돼(새로고침해도 안 사라짐) 상세 페이지에 배너로 뜬다.
    로컬 클론에서 직접(또는 AI가 CLI/MCP로) 충돌을 해결해 push한 뒤,
    그 결과 커밋 SHA를 입력하면 Gitea에 "수동으로 병합됨"으로
    기록된다(owner 전용, Gitea의 `manually-merged` 머지 옵션을 그대로
    씀).
  - **Reject/Close/Reopen**(editor 이상) - Gitea 자체엔 "거부" 개념이
    없어 이 앱이 별도로 추적한다(`disposition`: null/merged/rejected).
    Close는 머지·거부 여부와 무관하게 그냥 닫되, 그때까지 둘 다 선택된
    적이 없으면 자동으로 거부 처리된다. 거부된 PR도 Reopen 후 새
    커밋 + 머지로 결국 Accept까지 갈 수 있다(Reopen이 disposition을
    지움).
  - **PR 조회/대화/진행내역/머지·거부·닫기·재오픈은 CLI/MCP에도 열려
    있다**(`pr` 명령군) - git 저장소 관리 기능 중 유일한 CLI/MCP
    노출(브랜치 생성/저장소 연동 등 나머지는 여전히 웹 전용) - PR
    생성만은 브랜치 선택 UI와 결합돼 있어 계속 웹 전용이다. AI가 실패한
    머지를 직접 진단하고 수동 완료까지 처리할 수 있게 하기 위한 예외.

## 14. git push 훅 자동화 (대기열)

- `hook create/list/update/delete`로 프로젝트별 트리거 규칙(브랜치
  조건 + 프롬프트 템플릿)을 정의하면, 매칭되는 push마다 대기열에
  항목이 쌓인다. `update`는 넘긴 필드만 부분 갱신하고(생략한 필드는
  기존 값 유지), 트리거 브랜치를 빈 문자열로 주면 브랜치 제한을
  해제한다(모든 브랜치 매칭).
- 브랜치 조건에 `*` glob 패턴을 쓸 수 있다(예: `release/*`) - 한
  세그먼트 안에서만 와일드카드로 동작해 `/`는 안 넘는다(`release/*`가
  `release/1.0`엔 매칭되지만 `release/1.0/hotfix`엔 안 됨). `*`가
  없는 값은 지금까지처럼 정확 일치.
- 서버가 세션을 직접 스폰하지 않으므로, 그 프로젝트를 다음에 여는
  세션이 대기열을 확인(`hook queue`)·처리(`hook ack`/`hook done`)한다.
- push 이벤트 수신은 저장소별 웹훅이 아니라 **인스턴스 전체를
  커버하는 Gitea 시스템 웹훅 1개**로 이뤄진다(서버 부팅 시 자동
  등록·멱등) - `repository.name`만으로 어느 프로젝트의 어떤 종류
  저장소(self_hosted/mirror/work)인지 순수 파싱.
- 대기열 항목은 `pending`으로 30일 넘게 방치되면 매일 도는 주기
  워커가 자동으로 `expired`로 전이한다(삭제 아님 - `hook queue
  --status expired`로 계속 조회 가능). 이미 `acknowledged`된 항목은
  대상에서 제외(진행 중인 작업을 임의로 만료 취급하지 않음).

## 15. Gitea 사용자 계정 마스터링

- 설계자가 가입하면 이 시스템이 그 설계자의 Gitea 계정도 함께
  만든다(비밀번호는 시스템이 토큰처럼 발급해 암호화 보관 - 직접
  로그인하는 경로는 없음). 이름 충돌은 숫자 접미사로 자동 회피.
- 계정 생성 직후 그 설계자 전용 **Gitea Personal Access Token(PAT)**을
  발급 - ① 이 시스템의 웹 UI/CLI로 저장하면 그 설계자 자신의 Gitea
  신원으로 커밋되고, ② 프로필 화면에서 재발급·1회 노출한 값을 외부
  git 클라이언트(`git clone`/`git push`)에 직접 쓸 수 있다.
- 저장소별 접근 가능 여부는 프로젝트 `Member.role`과 동기화된 Gitea
  협업자 권한(owner→admin/editor→write/viewer→read)이 통제 - 멤버가
  아닌 설계자의 토큰으로는 Gitea 자신이 clone/push를 거부한다.

## 16. 템플릿 관리 (CLAUDE.md / SKILL.md)

- 관리되는 프로젝트에 배포할 `CLAUDE.md`/`SKILL.md` 내용을 팀/그룹/
  프로젝트 단위로 override(project→group→team→설치 전역 기본값 순
  상속, 더 구체적인 쪽이 우선).
- `template deploy` - 해석된 템플릿(상속 병합 결과)을 그 프로젝트의
  자체 호스팅 저장소 루트에 실제로 커밋.
- **변경 이력** - 이미 override가 있는 스코프를 다시 덮어쓰면 덮어써
  지기 직전의 내용이 자동으로 리비전으로 남는다(`template revisions`
  로 조회, 최초로 override가 생기는 시점엔 스냅샷할 이전 값이 없어
  리비전이 안 남음) - Document의 리비전과 같은 원리. 별도의 "복원"
  명령은 없고, 과거 리비전의 내용을 그대로 `template set`에 다시
  넘기면 복원된다.
- 3절의 "팀/그룹 스코프 DocType 제거"와는 완전히 별개 기능(이름이
  비슷해 보여도 혼동 금지).
- **웹 UI "템플릿" 조회 패널**(프로젝트 설정 탭) - 이 프로젝트에
  실제로 적용될 CLAUDE.md/SKILL.md 해석 결과(상속 병합 후 최종
  내용)를 파일별로 읽기 전용 뷰어(MonacoEditor readOnly)로 보여주고,
  어느 스코프의 override에서 온 값인지(설치 전역 기본값/팀/그룹/
  프로젝트) 칩으로 표시한다. 같은 패널에 "저장소에 배포" 버튼이
  있어 `template deploy`를 바로 실행할 수 있다(editor 권한 이상). 편집
  자체는 아직 CLI/MCP 전용(`template set`) - 웹에서는 조회+배포만
  가능하다.

## 17. 가이디드 마이그레이션

- `docs migrate scan <dir>` - `concept`(옛 3단계 구현) 스타일 YAML
  frontmatter + 마크다운 파일을 로컬에서 스캔해 후보 목록을 JSON으로
  출력(순수 로컬 동작, API 호출 없음). 흔한 옛 상태 어휘(`active`/
  `wip`/`obsolete` 등)를 표준 코드로 자동 제안한다(`--no-status-preset`
  으로 끌 수 있음) - 실제로 매핑이 일어난 항목만 `originalStatusCode`
  에 원본 값을 남겨 매니페스트만 보고도 뭐가 바뀌었는지 알 수 있다.
- 검토·수정한 매니페스트를 `docs migrate apply <projectId>
  manifest.json`으로 반영(일괄 자동 임포트 아님 - 오탐지된 문서 타입/
  링크를 설계자가 그 자리에서 고칠 기회를 줌).
- **재실행 안전장치**(`#migrate-idempotent`) - apply는 성공한 항목마다
  매니페스트 파일 자체에 `appliedTrackingCode`를 남기므로, 같은
  매니페스트를 다시 반영해도 이미 반영된 항목은 재생성하지 않고
  `alreadyApplied`로만 보고된다. 실패한 항목은 표시가 안 남아 자동으로
  재시도 대상이 된다(수작업으로 실패 항목만 골라내던 과정이 자동화됨).
  완전한 멱등성은 아니다 - 이미 반영된 항목의 링크는 재실행 시 다시
  확인하지 않는다. 옛 답변 대기 체크리스트는 텍스트로만 옮겨지고
  Question/Answer로 자동 변환되지 않는다.

## 18. 프로젝트 삭제 ("제한구역")

- 프로젝트 "설정" 화면의 danger-zone 스타일 섹션에서 owner(admin은
  예외)가 프로젝트를 완전히 삭제할 수 있다.
- 문서/코멘트/칸반/Q&A/멤버/문서타입/접근권한오버라이드/폴더/
  추적코드/메시지/push훅프롬프트/템플릿override/API키 등 그 프로젝트의
  모든 DB 데이터가 cascade로 삭제되고, 연결된 Gitea 저장소(자체
  호스팅이면 하나, 외부 연동이면 미러+작업 저장소 둘 다)까지 함께
  삭제된다.
- 되돌릴 수 없음 - 웹 UI는 `window.confirm()`으로 확인 후 실행.

## 19. 웹 UI 전반

- **CUD 버튼 권한별 숨김** - 생성/수정/삭제 버튼은 서버가 계산해
  내려주는 권한 필드(팀/그룹의 `isAdmin`, 프로젝트의 `myRole`/
  `canToggleHidden`, 문서의 `perm`, 댓글의 `canManage`)를 그대로
  `v-if`에 써서, 권한 없는 사용자에게는 애초에 안 보인다("시도 후
  403"이 아니라).
- **사이드바 문서 탐색기 + 프로젝트 툴바** - 프로젝트 안에 들어가면
  좌측 사이드바가 팀/그룹/프로젝트 목록 대신 그 프로젝트의 문서
  탐색기로 바뀐다(DocType별 그룹핑 트리, "+"로 인라인 문서 생성,
  현재 문서 강조, 실시간 반영). 본문 상단엔 홈/메시지/문서/소스
  코드/변경 추적/칸반 보드/설정/키 관리 탭 툴바가 공통으로 뜬다.
  사이드바 너비는 300px(문서 트리 항목이 좁은 폭에서 잘려 보이던 것을
  설계자 지시로 220px에서 넓힘).
- **홈 = 프로젝트 진행 상황 대시보드**(`#project-dashboard`, 설계자
  지시 - "문서와 현재 도구들로 진행 상황 파악이 힘드니 대시보드
  기능을 강화") - 이전엔 "최근 변경 문서/최근 코멘트/최근 발신
  메시지" 3개로 파편화돼 있던 걸 한 화면으로 통합했다:
  - **프로젝트 현황 통계 스트립**(맨 위) - 문서 상태별 개수 칩,
    미처리 메시지 개수(클릭하면 `/messages?status=active`로 이동),
    미답변 질의(open+pending) 개수, 칸반 컬럼별 카드 수 칩.
  - **정체된 문서**(있을 때만 노출) - 종료 상태(`archived`)가 아니면서
    기본 14일 넘게 안 바뀐 문서를 "N일째 변경 없음"과 함께.
  - "답변 대기 질문"/"즐겨찾기한 문서" 섹션은 그대로 유지.
  - **통합 최근 활동 피드**가 기존 3개 분리 섹션(최근 변경 문서/
    코멘트/발신 메시지)을 대체 - 문서 생성/수정, 질의 등록/답변,
    코멘트, 메시지, 칸반 카드 생성을 시간순으로 병합해 하나의
    타임라인으로 보여준다(추적 코드 자동 링크 - `TrackingCodeText`).
    홈에는 20건까지만(대시보드 위젯 고정값) - "더보기"를 누르면
    전용 페이지(`/projects/:id/activity`, `ProjectActivityView.vue`)
    에서 최대 100건까지 본다(`RecentCommentsView.vue`의 "최근 코멘트"
    더보기와 같은 관례 - 실제 페이지네이션 없이 더 큰 limit 하나로
    "더 보여준다").
  전부 새 집계 엔드포인트(`GET /api/projects/:id/dashboard`, CLI
  `docs dashboard`/MCP `project_dashboard`로도 동일 데이터 조회 가능)
  하나로 온다. 활동 전체 목록은 별도로 `GET /api/projects/:id/activity`
  (CLI `docs activity`/MCP `project_activity`)로도 조회 가능.
- **Monaco 에디터** - 문서 본문(마크다운)과 소스 코드 파일 편집·저장·
  커밋에 공통 사용. 문서는 기본이 렌더링된 읽기 모드("편집" 클릭 시
  전환).
- **실시간 변경 추적 뷰** - git 커밋 로그+diff(파일별 카드, 접기/
  펼치기)와 문서 버전 이력 diff를 새로고침 없이 갱신.
- **엔티티 선택기**(`EntityPickerDialog.vue`) - "이미 존재하는 것을
  고르는" 모든 자리(칸반 카드 근거, 질문 근거, 개별 문서 접근 권한
  대상, 멤버/팀장/그룹 관리자 추가 대상, 연관 소스 코드)가 체크박스
  선택 다이얼로그로 통일.
- **추적 코드/사용자 자동 링크** - 메시지/코멘트/질의 본문 속 추적
  코드는 클릭 가능한 링크로 자동 렌더링(다이얼로그로 미리보기),
  사용자 참조는 `[닉네임 #N]` 프로필 링크로.
- **다이얼로그 z-index 관리** - 중첩해서 열어도 항상 마지막에 연
  다이얼로그가 맨 위.
- 문서/메시지/소스 코드 디렉터리/git 로그/사이드바 문서 목록에
  페이지네이션 또는 무한 스크롤(전체를 한 번에 안 불러옴).
- XSS 새니타이즈(문서 본문 등 사용자 입력 렌더링 시).
- **다크 모드** - 사이드바 하단 토글로 라이트/다크/시스템 3단 전환
  (localStorage에 저장, 시스템 선택 시 OS 설정 변경도 실시간 추종).
  CSS 커스텀 프로퍼티 기반 테마 토큰(`App.vue`)을 전체 46개 뷰/
  컴포넌트가 사용(`#responsive-dark-mode` 1단계는 셸+대표 화면
  6개, 2단계에서 나머지 전부 전환 완료 - 이제 앱 전체가 다크 모드를
  지원한다). Monaco 에디터는 CSS를 안 따라가므로
  `monaco.editor.setTheme()`로 별도 반응형 연동.
- **반응형 레이아웃** - 768px 이하에서 셸 사이드바가 off-canvas로
  접히고(햄버거 토글 + 배경 오버레이), 프로젝트 탭 바는 가로 스크롤
  대신 **넘치면 다음 줄로 줄바꿈**한다(`flex-wrap: wrap` - 탭 개수가
  늘어나도 스크롤 없이 항상 전부 보임). 모달 다이얼로그는 원래부터
  `width: min(Npx, 90vw)`로 좁은 화면에 이미 대응돼 있었음. 앱
  셸(`.content`)은 기본 폭이 960px로 제한돼 있지만, **관계도/칸반
  보드는 route meta(`fullWidth`)로 이 제한을 풀어 화면을 가득
  채운다**(`#relations-kanban-full-width` - 960px보다 더 넓히지는
  않음, `max-width: 100%`일 뿐). 앱 셸(`.content`)이 뷰포트 높이를
  채우고 각 화면이 그 안에서 스스로 스크롤하는 구조라, 칸반 보드처럼
  "남은 높이를 다 쓰고 그 안에서 가로/세로로 스크롤"하는 화면도
  자연스럽게 구성 가능.

## 20. CLI / MCP

- **완전성 원칙** - 클로드가 문서/워크플로우 상태를 읽거나 쓰는 모든
  동작은 CLI(`docs`) 명령과 MCP(`docs-mcp`) 도구로 1:1 대칭 존재해야
  한다. 둘 다 REST API만 호출하는 순수 클라이언트(core 직접 호출
  안 함).
- **의도적 예외**(완전성 원칙에서 제외되는 것 - 버그 아님):
  - `auth register/login/logout`, `auth use-key` - 비밀번호/API 키가
    대화 컨텍스트에 남지 않도록 CLI 전용(`auth_whoami`만 MCP 예외).
  - 코멘트 - 설계자 전용 채널이라 CLI/MCP 어디에도 없음.
  - 폴더 - AI가 몰라도 되는 개인 정리 기능이라 개념 자체가 없음.
  - API 키 관리(`key create/list/revoke`), `git my-token` - 신원/
    자격증명 관리 동작이라 CLI 전용.
- **`npm run audit:cli-mcp`**(backend) - 위 완전성 원칙을 사람이
  매번 수작업으로 대조하지 않도록 자동화한 스크립트. CLI는 소스
  정적 파싱, MCP는 서버를 실제로 띄워 `tools/list`를 호출해 실측
  비교하고, 의도적 예외 목록을 벗어난 비대칭이 있으면 0이 아닌
  종료 코드로 실패한다.
- 로컬 스크래치 사본(편집용 임시 파일)은 정상 작업 방식이지만, 그
  사본을 프로젝트의 git 저장소에 커밋하지 않는다(문서 정본은 시스템
  DB).
- `docs auth login`으로 저장한 자격증명(`~/.claude-native-workflow/
  credentials.json`)을 CLI와 MCP가 공유.
- **목록 명령 페이지네이션** - 배열을 반환하는 명령 대부분이
  `--page`/`--count`(MCP는 `page`/`pageSize`)를 옵션으로 받는다 -
  생략하면 기존처럼 전체 배열(문서 목록은 최대 1000건), 주면
  `{items, page, pageSize, total, totalPages}` 응답으로 바뀐다(git
  로그만 Gitea 제약으로 total 없이 `{items, hasMore}`).
- **문서 부분 읽기/검색/버전 비교**(`#document-partial-read-grep-diff`)
  - 파일에 대한 Read/Grep 도구가 하는 일을 문서 본문에 대해 한다.
  `docs read <trackingCode> [--offset][--limit]`(줄 범위 읽기),
  `docs grep <trackingCode> <pattern> [--case-insensitive][--context]`
  (정규식 줄 검색), `docs diff <trackingCode> <from> [<to>]`(리비전
  id 또는 `current` 두 시점을 줄 단위로 비교, `diff` 라이브러리 사용) -
  셋 다 CLI/MCP(`document_read`/`document_grep`/`document_diff`)
  대칭.
- **소스 코드 파일 부분 읽기/검색** - 위와 같은 목적으로 소스 코드
  파일에도 적용. `docs git read <projectId> <path> [--ref][--offset]
  [--limit]`, `docs git grep <projectId> <path> <pattern> [--ref]
  [--case-insensitive][--context]` - CLI/MCP(`git_read`/`git_grep`)
  대칭(버전 비교는 이미 `docs git log/diff/show`로 되므로 따로
  안 만듦).
- **grep 패턴 컴파일의 POSIX 문자 클래스 지원 + 리터럴 폴백**
  (`#grep-posix-classes`) - `docs grep`/`docs git grep` 둘 다 공유하는
  패턴 컴파일 단계(`textLines.ts`의 `grepLines()`/`compilePattern()`)가
  더 이상 "잘못된 정규식" 에러를 던지지 않는다:
  1. `[[:alpha:]]`/`[[:digit:]]`/`[[:alnum:]]`/`[[:upper:]]`/
     `[[:lower:]]`/`[[:space:]]`/`[[:blank:]]`/`[[:punct:]]`/
     `[[:cntrl:]]`/`[[:print:]]`/`[[:graph:]]`/`[[:xdigit:]]` 같은
     셸 grep/ripgrep 스타일 POSIX 문자 클래스를 **항상 먼저**(예외
     발생 여부와 무관하게) 동등한 JS 문자 범위로 치환한다(예:
     `[[:alpha:]]` → `[A-Za-z]`) - JS RegExp는 이 문법을 몰라도
     에러를 던지지 않고 조용히 다른 뜻으로 오해석하기 때문에
     (`[[:alpha:]]`가 ":alpha" 리터럴 문자들 + 홑 `]`로 잘못 파싱됨),
     try/catch로 감지해 나중에 치환하는 방식으로는 이 케이스에
     절대 도달할 수 없다 - 반드시 무조건 먼저 치환해야 한다.
  2. (치환됐든 안 됐든) 그 결과를 JS 정규식으로 컴파일 시도.
  3. 그래도 진짜 문법 오류(괄호 미종료 등)면 정규식이 아니라
     순수 리터럴 문자열 검색으로 폴백한다(원본 패턴 기준, 특수문자
     이스케이프, 대소문자 옵션은 그대로 적용) - 이제 `grepLines`는
     패턴 관련 이유로는 절대 에러를 던지지 않는다.
- **문서 색인 조회는 본문을 안 싣는다**(`#document-list-lightweight`)
  - `docs list`/`document_list`(`--type`/`--status` 필터 가능)는
  trackingCode/title/docTypeId/statusCode 등 요약 필드만 반환하고
  본문은 안 준다 - "무엇이 있는지 훑어보고 고른 뒤 필요한 것만
  get/read/grep으로 본다"는 흐름에 맞춘 것. 본문이 필요하면 이어서
  `docs get`(전체) 또는 `docs read`/`docs grep`(부분)을 쓴다.
  `--page`/`--count` 없이 부르는 기본 호출은 문서 1000건까지만 한
  번에 담을 수 있는데(Meilisearch 기본 한도), 실제로 1000건을 넘는
  프로젝트면 잘린 목록을 조용히 주지 않고 페이지네이션을 쓰라는
  명확한 에러로 거부한다(`#document-list-page-required-at-scale`).

## 21. 인프라 & 배포

- **Prisma 스키마 3드라이버**(PostgreSQL/MySQL/SQLite) - 완전
  정규화(JSON 컬럼 없음), 셋 다 모델이 동일(드라이버별 타입
  어노테이션만 다름).
- **Docker Compose 스택** - nginx + backend + Postgres + Meilisearch +
  EMQX + Gitea. Meilisearch/EMQX 키는 외부에서 발급받는 게 아니라
  설치자가 직접 정하는 값(부트스트랩 파일로 기동 시 자동 등록 -
  대시보드 로그인 불필요). Gitea의 최초 관리자 계정/PAT 발급도 웹
  설치 마법사 없이 `docker exec` 기반 CLI로 전부 자동화됨
  (#gitea-nginx-lockdown).
- **nginx가 유일한 기본 호스트 노출 지점**(#gitea-nginx-lockdown) -
  `.git`로 끝나는 git smart-HTTP 요청은 Gitea로, `/api`는 backend로,
  나머지는 frontend로 라우팅한다. backend/Gitea/frontend 자신의 포트는
  기본적으로 호스트에 노출되지 않는다(로컬 개발 편의를 위한 주석
  처리된 오버라이드만 존재).
- **frontend는 자기 자신의 nginx로 빌드 결과물을 정적 서빙하는 별도
  compose 서비스**다(#frontend-own-service - backend와 독립적으로
  빌드/재기동됨, backend 이미지엔 frontend 코드가 안 들어간다).
- **데이터는 Docker 네임드 볼륨이 아니라 `./data/` 바인드 마운트에
  저장**(#bind-mount-data-dir - postgres/gitea/meilisearch/emqx 전부)
  - Docker 엔진 자체를 리셋(Docker Desktop의 "Reset to factory
  defaults" 등)해도 호스트 디스크의 평범한 폴더라 그대로 남는다.
  `.gitignore`로 저장소엔 커밋 안 됨.
- 호스트 직접 설치 경로도 지원(Docker 없이 Node.js + 외부 Meilisearch/
  EMQX) - 이 경로에선 frontend를 쓰려면 별도 정적 서버+리버스 프록시가
  필요하다(README.md 참고).

## 22. 코드 관계도 (Code Relation Graph)

- Claude가 코드를 탐색하며 스스로 파악한 "무엇이 어디서 왜 참조되는지"
  를 직접 기록해두는 자기 기록형 그래프 - 다음 세션이 같은 탐색을
  반복하지 않고 바로 찾아 쓴다. 폴더와 동일한 원칙으로 **프로젝트 내
  설계자(로그인 계정)별로 완전히 독립적** - 다른 설계자의 관계는
  전혀 안 보인다.
- 각 관계는 대상(target)/참조 주체(referrer)/목적(purpose)/파일 경로+
  줄·열/임의 JSON 데이터/태그(다중)/**연관 문서 추적코드(다중, 실제
  존재하는 문서만 지정 가능)**를 담는다.
- **상위/하위 관계는 다중 부모를 지원하는 범용 그래프**(단일 부모
  트리 아님) - 한 관계가 여러 상위 개념의 자식으로 동시에 걸릴 수
  있다. 순환도 허용한다(실제 코드 관계는 상호 참조가 흔함) - 순회할
  때만 방문 집합으로 안전하게 처리된다.
- CRUD 전체 + bulk(추가/수정/삭제, 항목별 부분 성공 결과) - CLI
  `docs relation ...`, MCP `relation_*` 도구, 이 저장소 자신의
  SKILL.md에도 명세돼 있어 Claude가 스스로 언제/어떻게 써야 하는지
  안다.
- **관계도 초기화**(`relation reset`/`relation_reset`, 웹 UI "관계도
  초기화" 버튼) - 이 설계자 본인의 관계를 브랜치 기준으로 일괄
  삭제한다(모든 브랜치/브랜치 없음/특정 브랜치 중 선택). 웹 UI는
  삭제 전 어떤 범위를 지울지 확인하는 다이얼로그를 띄운다 - 브랜치
  스코프의 웹훅 기반 자동 정리(deleteRelationsForBranch, 브랜치
  삭제 시 전체 설계자 대상)와 달리 이건 설계자 본인이 명시적으로
  요청하는 동작이라 항상 본인 소유 관계만 지운다.
- **연관 문서 추적코드는 텍스트 입력이 아니라 선택기로 고른다** -
  관계 추가/수정 다이얼로그에서 "추적코드 선택" 버튼이 기존 문서
  선택기(EntityPickerDialog, 검색+다중 선택)를 열어 실제 존재하는
  문서만 고를 수 있다 - 선택된 추적코드는 칩으로 표시되고 개별
  제거도 가능하다.
- 부모/자식 방향 깊이 제한 순회(`ancestors`/`descendants`, 기본
  depth 3·최대 20) - 태그/검색어로 필터링 가능. 필터 없이 프로젝트
  전체 관계를 한 번에 불러오는 **"전체 보기" 토글**도 있다 -
  무제한 로드는 하지 않고 항상 500건 상한을 걸어 호출하며, 그 상한을
  넘는 프로젝트는 "일부만 표시됩니다" 안내가 뜬다.
- **웹 UI "관계도" 탭 - Cytoscape.js 기반, 전환 가능한 두 가지 뷰
  모드**(`#relations-graph-redesign`):
  - **네트워크형**(기본값) - `fcose` 힘-방향 레이아웃, 원형 노드를
    태그별로 색상 구분해 클러스터처럼 보여준다.
  - **계층형** - `dagre` 레이아웃으로 부모→자식 방향을 위에서
    아래로 정돈해서 보여준다(사각형 노드, 태그 색상 없이 검색/필터
    매칭만 강조).
  - 두 모드 모두 검색/태그/파일/추적코드 필터, 노드 클릭 시 상세
    패널, 더블클릭/버튼으로 깊이별 펼치기는 동일하게 동작한다.
  - **확대/축소/100%/화면에 맞추기 버튼**이 캔버스 우하단에 항상
    떠 있다 - 마우스 휠 확대/축소도 가능하고, 펼치기로 노드가
    늘어나도 지금 보고 있는 확대/이동 상태가 유지된다(검색/필터
    변경이나 뷰 모드 전환처럼 그래프 자체가 바뀔 때만 화면을 다시
    맞춘다).
  - 문서 미리보기 다이얼로그의 "관계도에서 보기", 소스 코드 화면의
    "관계도" 버튼으로 문서/파일 쪽에서 정확히 필터링해 진입할 수
    있는 것도 그대로.
  - **태그 공유 연결선(`#relation-graph-tag-edges`)** - 상위/하위
    관계(DB의 실제 부모/자식 엣지)는 기존 그대로 화살표 있는 실선.
    거기에 더해, **태그를 하나라도 공유하는 노드끼리는 화살표 없는
    점선**으로도 이어진다(방향 없는 관계라 화살표 없음 - 설계자
    지시, 처음엔 실선으로 지시했다가 점선으로 정정됨). 이미 부모/
    자식으로 직접 이어진 쌍은 점선을 겹쳐 그리지 않는다(중복 방지).
    현재 그래프에 로드된 노드들 안에서만 계산(전체 프로젝트 전수
    비교 아님) - 태그→노드ID 역색인으로 짝지어 로드된 노드 수가
    늘어도(최대 "전체 보기" 500개) 감당 가능한 비용으로 계산한다.
- **브랜치 스코프** - CLI/MCP가 실행 디렉터리의 실제 로컬 git
  저장소에서 `git rev-parse --abbrev-ref HEAD`로 현재 체크아웃된
  브랜치를 그때그때 자동 감지해 그 브랜치로 관계를 태깅/필터링한다(별도
  "브랜치 전환" 명령이나 저장 상태 없이, `git checkout`으로 바꾸면
  다음 호출부터 자동 반영). `--branch`로 명시 지정, `--all-branches`로
  브랜치 구분 없이 전체 조회도 가능. **그 브랜치가 Gitea에서 삭제되면**
  (시스템 웹훅의 `delete` 이벤트) **그 브랜치로 태깅된 관계가 설계자
  구분 없이 프로젝트 전체에서 일괄 삭제된다** - "지금 탐색 상태"라
  사라진 브랜치를 계속 가리키면 오히려 오해를 부르기 때문(개인 소유
  스코핑의 유일한 의도적 예외).

## 23. 보류 계획 (별도 계획 체크리스트)

- Document/DocType/DocStatus 체계와 완전히 별도로 관리되는 독립
  엔티티("계획") - Claude가 작업 중 "이건 나중에 따로 계획을 잡아야
  한다"고 판단한 항목을 모아두는 체크리스트다. 트래킹 코드 접두어는
  `PN`.
- 필드는 트래킹 코드, 제목, 본문(Markdown), 관련 문서(추적 코드
  목록, 순서 없음), **선행 조건**(다른 계획들에 대한 의존성, 여러 개
  가능, 순서 없음), 상태 6개 고정값 - `계획됨`(planned)/
  `승인대기`(pending_approval)/`검토중`(in_review)/`예정`(scheduled)/
  `완료`(completed)/`거부`(rejected). DocStatus와 달리 프로젝트별로
  커스터마이즈되지 않으며, 상태 전이는 그래프로 제약되지 않고 언제든
  6개 중 아무 값으로나 바꿀 수 있다. 선행 조건도 실행 순서를 강제하는
  그래프가 아니라 순수 참조 목록이라 순환을 막지 않는다 - 자기 자신을
  선행 조건으로 지정하는 것만 거부된다.
- CLI: `docs plan new|list|statuses|get|set|status|delete|link|
  unlink|depend|undepend`. MCP: `plan_new`/`plan_list`/
  `plan_statuses`/`plan_get`/`plan_set`/`plan_status`/`plan_delete`/
  `plan_link`/`plan_unlink`/`plan_depend`/`plan_undepend`. REST:
  `/api/projects/:projectId/plans`, `/api/plans/:trackingCode[/status|
  /refs[/:code]|/dependencies[/:code]]`.
- 웹 UI: 프로젝트 네비게이션의 "계획" 탭 - 목록 화면(상태 필터/검색/
  생성 - 생성 시 관련 문서/선행 조건도 같이 지정 가능)과 편집 화면
  (제목/상태/본문 편집, 관련 문서·선행 조건 추가/삭제)을 제공한다.
  관련 문서/선행 조건 선택은 항상 검색 기반 선택기 다이얼로그
  (`EntityPickerDialog`, `kind: "document"`/`"plan"`)로 하고, 추적
  코드를 직접 타이핑하는 입력은 없다 - 선행 조건 선택기는 편집 중인
  계획 자기 자신을 목록에서 제외한다(`excludeKeys`). 메시지/코멘트/
  문서 본문 등에 등장하는 `PN-XXXXXXXX` 코드를 클릭하면 미리보기
  다이얼로그가 뜬다(질의/칸반 카드 코드와 같은 방식).
- **bulk 명령**(웹 UI 없음, CLI/MCP 전용) - `plan bulk-export
  <projectId> <outFile> [--status] [--q]`(조건에 맞는 전체를 페이지
  상한 없이 로컬 JSON 파일로, MCP `plan_export`는 파일 저장 없이
  배열만 반환)/`plan bulk-import <projectId> <file>`(로컬 JSON
  파일의 여러 계획 정의를 한 번에 생성, MCP `plan_bulk_import`는
  `items` 배열을 직접 받음 - 항목별 성공/실패 반환, `refs`/
  `dependsOn`은 이미 존재하는 문서/계획만 가리킬 수 있고 같은 배치
  안 다른 항목은 못 가리킴)/`plan status-bulk <status>
  <trackingCode...>`·`plan link-bulk <docTrackingCode>
  <trackingCode...>`·`plan depend-bulk <dependsOnTrackingCode>
  <trackingCode...>`(같은 값을 여러 계획에 한 번에 적용, 문서
  `bulk-transition`과 같은 관례 - 항목별 결과 반환).
