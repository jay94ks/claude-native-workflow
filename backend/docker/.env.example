# docker-compose.yml이 읽는 시크릿. 이 파일을 .env로 복사한 뒤 실제 값을
# 채운다(.env 자체는 커밋 대상이 아님 - 저장소 .gitignore 확인).

POSTGRES_PASSWORD=change-me

# 최소 32자(무작위) 필요 - 짧으면 기동 자체가 실패한다. 예:
# `openssl rand -hex 32`
JWT_SECRET=change-me-to-a-random-string-at-least-32-characters-long

# 정확히 32바이트를 hex 64자로 인코딩한 값이어야 한다 - 짧거나 길면
# 기동 자체가 실패한다. `openssl rand -hex 32`로 생성.
CREDENTIAL_ENCRYPTION_KEY=change-me-64-hex-chars-openssl-rand-hex-32

MEILISEARCH_API_KEY=change-me

# EMQX는 최초 기동 후 대시보드(http://localhost:18083, 기본 admin/public)
# 에서 API Key를 발급받아 채워야 한다(docker-compose.yml의 emqx 서비스
# 주석 참고) - 이 두 값을 채우기 전까지는 backend가 뜨긴 하지만 실시간
# 발행(realtimePublish)만 조용히 스킵된다.
EMQX_API_KEY=
EMQX_API_SECRET=

# Gitea도 EMQX와 같은 패턴 - 최초 기동 후 웹 UI(http://localhost:3001)
# 에서 설치 마법사를 완료하고 관리자 계정을 만든 다음, Settings →
# Applications에서 repo 권한 Personal Access Token을 발급해 채운다.
# 저장소는 전부 이 관리자 계정 네임스페이스 아래 생성된다. 세 값을
# 채우기 전까지는 backend가 뜨긴 하지만 git 저장소 연결/이력 조회
# 엔드포인트가 전부 에러를 반환한다.
GITEA_ADMIN_USERNAME=change-me
GITEA_API_TOKEN=

# 웹훅 콜백 주소로 쓰인다(Gitea/GitHub/GitLab이 push 이벤트를 이 주소로
# 보낸다) - 외부에서 실제로 닿을 수 있는 주소여야 한다(로컬 전용 개발
# 환경이면 채우지 않아도 나머지 기능엔 지장 없음, git/link 시 웹훅
# 등록만 실패).
PUBLIC_BACKEND_URL=http://localhost:8760
