# 기능별 사용 시나리오 + QA 체크리스트 + 추가 개발 계획

이 문서는 README.md(현재 기능 요약)/DESIGN-NOTES.md(라운드별 변경
기록)와 성격이 다르다 - "무엇을 만들었나"가 아니라 **"실제로 어떻게
쓰이고, 그걸 어떻게 검증하고, 다음엔 뭘 보강할까"**를 기능 영역별로
정리한 살아있는 참고 문서다. 각 영역은 세 절로 구성된다:

- **사용 시나리오**: 설계자/클로드가 실제로 이 기능을 쓰는 구체적인
  상황(사용자 스토리 형태).
- **QA 체크리스트**: 그 시나리오를 검증하는 구체적 절차. 이번 세션
  중 스크래치 스택으로 **실제로 실측 확인된 항목은 `[x]`**, 아직 이
  형태로 정식 검증되지 않은 항목은 `[ ]`로 구분한다(실측 안 됐다고
  버그가 있다는 뜻은 아니다 - "아직 이 문서 형식으로 재확인 안 함"일
  뿐).
- **추가 개발 계획**: 시나리오를 훑으며 드러난 개선 여지는 여기엔
  태그만 남긴다 - 전체 설명·우선순위는 [PLANS.md](PLANS.md)에서
  한 곳에 모아 관리한다(전부 "승인 대기" 상태 - 설계자가 우선순위를
  정하면 그때 각각 Plan Mode로 상세 설계를 잡는다).

---

## 1. 인증 & 신원 (로그인/회원가입/프로필/API 키 3종)

### 사용 시나리오
- 설계자가 새 설치에 처음 접속해 자동 생성된 `admin`/`12345678`로
  로그인하고 비밀번호를 그대로 방치한 채(또는 바꾼 채) 팀을 꾸린다.
- 클로드 세션이 `docs auth login`으로 CLI 자격증명을 저장해두고,
  이후 세션들은 재로그인 없이 그 자격증명 파일을 공유해 계속 작업한다.
- 자동화 스크립트(CI, 개인 봇)가 사람 로그인 없이 **개인 키**로 CLI를
  구동해 특정 몇 개 프로젝트만 건드린다.
- 팀장이 팀 전체를 관리하는 **팀 관리 키**를 발급해 외부 도구(사내
  대시보드 등)에 넘긴다.
- 시스템 관리자가 `admin` 계정으로 로그인해, 다른 설계자가 만든
  팀/그룹/프로젝트나 남의 코멘트·API 키까지 필요시 직접 개입해 처리한다
  (분쟁 조정, 방치된 자원 정리 등).

### QA 체크리스트
- [x] 회원가입/로그인/로그아웃/토큰 갱신 왕복(Phase 0 검증).
- [x] 최초 설치 시 계정 0개면 `admin`/`12345678` 자동 시드, 이미 계정이
  있으면 건드리지 않음.
- [x] **`admin` 계정은 최고 관리자** - 역할/관리자 판정(프로젝트
  owner/editor/viewer, 팀장, 그룹 관리자) 전부를 실제 멤버십/등록
  없이도 우회, 스코프가 좁혀진 API 키(팀/프로젝트 단위)로도 그 스코프
  밖의 완전히 다른 프로젝트에 접근 가능(동일한 모양의 일반 설계자 키는
  여전히 403 - 대조 확인), 코멘트 수정/개인 폴더 관리/git 자격증명
  삭제처럼 본인 소유물만 다루게 설계된 기능도 타인 것까지 대행 가능 -
  전부 실측.
- [x] 개인 키로 로그인과 동등하게 여러 프로젝트에 접근 가능, 프로젝트
  키는 그 프로젝트 하나로 한정, 팀 키는 그 팀 소속 프로젝트 전부로
  한정(다른 프로젝트/팀은 403·목록에서도 제외) - API 키 라운드에서
  실측.
- [x] 범위가 좁은 키로 키 관리 자체(`key create`/`revoke`)·프로필
  수정·팀/그룹/프로젝트 생성 시도 시 403(권한 상승 방지) - 실측.
- [x] 키 배제(soft revoke) 후 그 키로 호출 시 401, DB 행은 남고
  `status=revoked`만 바뀜 - 실측.
- [x] 키 생성 응답에만 원문이 오고, 이후 목록 조회는 항상 마스킹 -
  실측(웹 UI 1회 노출 플로우 포함).
- [x] API 키 선택적 만료(TTL) - 생성 시 과거/잘못된 형식의 만료 시각은
  400, 미지정은 무기한(기존 동작 유지) 확인. 만료 전엔 정상 사용,
  만료 후엔 401 + 목록에서 `status=expired`로 표시, 만료된 키도
  정상 배제 가능한지 확인. CLI `key create --expires-in <일수>`와
  웹 UI(개인/프로젝트/팀 키 관리 세 화면 전부) 생성 폼의 만료 선택
  드롭다운까지 실측(docker 스택 재빌드해 실제 HTTP 호출·브라우저
  양쪽으로 확인) - 이번 세션에서 실측.
- [x] JWT access 토큰 만료(15분) 후 refresh 토큰으로 자동 갱신 -
  웹 프론트는 이미 구현/실측 확인됨. **CLI/MCP는 refresh_token을
  저장만 하고 실제로 쓰는 코드가 없었던 것을 이번에 발견해 수정**
  (`#cli-token-refresh`) - access 토큰을 일부러 훼손해 401을 재현한
  뒤 자동 갱신+재시도+토큰 회전이 실제로 되는지, 두 토큰 모두
  무효일 땐 무한 재시도 없이 에러로 끝나는지 실측.
- [x] **본인 비밀번호 변경(`#self-service-password-change`)** - 실사용
  중 이 기능이 없다는 걸 설계자가 직접 발견해 추가 요청 - 이전
  라운드에선 범위 밖으로 확인만 하고 넘어갔던 항목이 실제로 필요한
  것으로 확정돼 구현됨. `POST /api/auth/me/password`(현재 비밀번호
  검증 → 최소 8자 검증 → 변경)를 틀린 현재 비밀번호(400)/너무 짧은
  새 비밀번호(400)/정상 변경(200) 세 경우 다 HTTP로 직접 실측, 변경
  후 옛 비밀번호 로그인 거부+새 비밀번호 로그인 성공까지 확인. 웹
  UI "내 정보" 화면의 "비밀번호 변경" 폼(새 비밀번호/확인 불일치
  시 서버 호출 전에 클라이언트에서 막는지)도 브라우저로 실제 로그인
  → 변경 → 성공 메시지까지 왕복 확인. CLI `auth change-password`는
  `auth register/login/logout`과 같은 이유로 MCP엔 의도적으로 없음
  (`audit-cli-mcp.ts`의 `KNOWN_CLI_ONLY`에 등록).
- [x] admin 대행 비밀번호 재설정 - 일반 계정으로 `GET
  /api/admin/users`/`POST .../reset-password` 호출 시 403, admin
  으론 200 확인. 재설정 후 기존 비밀번호로 로그인 불가, 새 임시
  비밀번호로는 즉시 로그인 가능한지 확인. `GET /api/auth/me`의
  `isSuperAdmin` 필드가 admin=true/일반=false로 정확한지 확인. 웹
  UI(비-admin은 "사용자 관리" 네비 링크 자체가 안 보이고, `/admin/
  users` 직접 접근 시 안내 문구만 표시 + 실제 차단은 서버 403)와 CLI
  `docs user list`/`user reset-password` 왕복까지 실측(docker 스택
  재빌드해 실제 HTTP 호출·브라우저 양쪽으로 확인) - 이번 세션에서
  실측.
- [x] 로그인 무차별 대입 방어(계정별 5회/15분, IP별 20회/15분 이중
  잠금) - 틀린 비밀번호 연속 실패 시 429 + `Retry-After` 확인, 잠긴
  상태에서 올바른 비밀번호도 거부되는지, 성공 시 계정 카운터만
  리셋되는지, 잠금 만료 후 재시도 가능한지, IP별 잠금이 서로 다른
  계정에도 적용되는지 전부 실측(docker 스택 재빌드해 실제 HTTP 호출로
  확인) - 이번 세션에서 실측.
- [x] 사용자 관리 화면 검색/페이지네이션/소속 조회+강제 방출
  (`#user-membership-management`) - 검색 디바운스+부분일치, 페이지
  왕복, 사용자명 클릭 시 프로필 이동, 여러 팀/그룹에 걸친 사용자의
  소속 다이얼로그 정확도, 유일한 owner/관리자는 방출 버튼 비활성+
  툴팁, 공동 관리자가 있는 관계는 실제 방출 성공, 그룹의 부모 팀에
  팀 관리자가 있을 때 그룹 유일 관리자 방출이 허용되는 상속 예외
  케이스까지 전부 실측(docker 스택 재빌드 + 실제 브라우저) - 이번
  세션에서 실측.

---

## 2. 팀/그룹/프로젝트 계층 + 숨김/팀장 + 계층별 멤버 가시성 + 기본 비공개

### 사용 시나리오
- 설계자가 회사 조직을 팀으로 나누고, 팀마다 프로젝트 그룹을 만들어
  관련 프로젝트를 묶는다.
- 소속되지 않은 팀/그룹/프로젝트는 목록에서 아예 안 보여야 한다 -
  단 관리자가 "공개"로 지정하면 그 팀/그룹은 누구나 볼 수 있고,
  프로젝트는 공개로 지정해도 그 프로젝트가 속한 그룹에 실제
  멤버십이 있는 사람에게만 보인다.
- 팀장이 민감한 프로젝트를 "숨김" 처리해 그 팀 멤버가 아닌 사람에게는
  존재 자체가 안 보이게 한다.
- 팀장이 휴가 중인 다른 팀장 대신 어떤 프로젝트의 숨김을 해제해줘야
  한다.
- 그룹/팀 관리자가 자기 산하 전체 프로젝트에 누가 소속돼 있는지 한
  화면에서 확인한다(프로젝트를 일일이 열어보지 않고).
- 실수로 만든 빈 팀/프로젝트 그룹을 정리한다.
- 프로젝트 owner가 더 이상 안 쓰는 프로젝트를 문서/코멘트/칸반/Q&A/
  연결된 git 저장소까지 통째로 정리한다("제한구역"에서 완전 삭제).

### QA 체크리스트
- [x] 팀 생성 → 팀 소속 그룹 생성 → 그룹 소속 프로젝트 생성 계층
  왕복(여러 라운드에 걸쳐 반복 검증).
- [x] 프로젝트 숨김 시 비멤버에게 목록에서 제외, 기존 멤버·팀장에게는
  계속 노출 - 실측(팀장 여부를 실제 이중 팀 관리자 계정으로 강하게
  재현).
- [x] 팀장이 숨김을 해제하면 비멤버도 다시 목록에 보임 - 실측.
- [x] `InstallConfig.teamsEnabled`를 꺼도 팀 메뉴/기능이 안전하게
  숨는지(여러 라운드에 걸쳐 회귀 확인).
- [x] 팀/프로젝트 그룹 CRUD(이름 수정, 팀은 `enabled` 토글, 비어있지
  않으면 삭제 거부·비운 뒤 삭제 성공) - 3계정(설계자별 서로 다른
  역할)으로 REST 직접 호출 + 웹 UI 클릭 양쪽 실측(프로젝트 자체의
  삭제는 여전히 없음 - 숨김만 지원, 별개 기능).
- [x] 그룹/팀 멤버 가시성(그 산하 전체 프로젝트 멤버 합)을 그
  관리자만 볼 수 있는지 - 그룹 생성자 자동 관리자 등록, 팀장의
  그룹 관리자 권한 자동 상속(등록 없이), 관리자도 아니고 프로젝트
  멤버도 아닌 제3자는 403, 관리자를 새로 추가하면 그 사람도 조회
  가능해지는지까지 전부 실측.
- [x] 팀장/그룹 관리자 등록·해제 라우트가 "실제 관리자인지"를
  확인하는지(과거엔 API 키 스코프만 확인해 아무 설계자나 자기
  자신을 팀장으로 등록할 수 있었던 허점 - 비관리자 자가 등록 시도
  → 403으로 수정 확인).
- [x] `TeamAdmin`/`ProjectGroupAdmin`이 `onDelete: Cascade` 없이
  붙어있어 관리자 행이 하나라도 있으면(자동 등록으로 항상 있음)
  "빈 팀/그룹" 삭제가 FK 제약으로 항상 실패하던 회귀 - 발견 즉시
  수정 후 재현 확인.
- [x] **프로젝트 완전 삭제("제한구역")** - owner 계정으로 문서/코멘트/
  질의/칸반카드/git 저장소가 있는 실제 프로젝트를 삭제 → DB 데이터가
  cascade로 전부 사라지는지, 연결된 Gitea 저장소가 실제로 삭제됐는지
  Gitea API로 직접 확인. owner 아닌 멤버는 웹 UI에 "제한구역" 섹션
  자체가 안 보이고 `DELETE .../projects/:id` 직접 호출도 403인지
  확인. `docs project-delete`/MCP `project_delete` 왕복 확인. **실측
  중 발견한 스키마 버그**: `DocStatus`/`DocStatusTransition`/
  `Document`(docType·status FK)/`KanbanCard`(column FK)/`Folder`
  (자기참조 parentFolder FK)에도 `TeamAdmin`과 같은 종류의 cascade
  누락이 있어 첫 삭제 시도가 FK 위반으로 실패 - 재현 후 8개 관계
  추가로 수정.
- [x] **프로젝트 그룹 재소속** - 그룹 관리자가 목적지 팀의 팀장이
  아닌 채로 다른 팀 이동을 시도하면 403 확인, 두 팀 다 관리하는
  계정으로는 성공(산하 프로젝트가 새 팀 목록에 자동으로 나타나는지
  확인)하는지 실측. 팀 없음으로 떼어내는 건 목적지 동의가 필요 없는지,
  떼어낸 직후 팀 상속으로만 관리자였던 계정은 그 그룹에 대한 권한을
  잃는지(관리자 상속이 계산값이라 즉시 바뀌는 것 확인) - 둘 다 실측.
  REST 직접 호출 + CLI `group-update --team` + MCP `group_update`
  (teamId) + 웹 UI 드롭다운(성공/인라인 에러 둘 다) 전부 실측.
- [x] **팀/그룹/프로젝트 기본 비공개 가시성** - 소속 없는 제3자
  계정에게 비공개 팀/그룹/프로젝트가 목록에 전혀 안 보이는지, 각각
  `isPublic=true`로 바꾸면(팀/그룹은 그것만으로, 프로젝트는 그 그룹에
  실제 멤버십이 있는 사람에게만) 보이는지 실측. `hidden=true`가
  `isPublic`보다 우선 적용돼 공개 프로젝트도 멤버/팀장/그룹 관리자
  외엔 다시 안 보이는지, 목록 가시성과 `GET /api/projects/:id`(단건)
  가 일치하는지, 그룹 관리자가 멤버가 아닌 산하 프로젝트도 보는지
  (새로 보정한 관리자 우회) 전부 실측. CLI `--public` 옵션(전 명령)
  + MCP `isPublic` 인자/`project_public` + 웹 UI 세 화면(체크박스·
  배지·토글) 전부 실측.

---

## 3. 멤버십 & 세부 접근 권한 (역할 + 3단계 오버라이드)

### 사용 시나리오
- 프로젝트 owner가 외주 설계자를 editor로 초대하되, 특정 민감한
  문서 타입(예: 결정 문서)은 읽기만 허용하고 쓰기는 막는다.
- 프로젝트 owner가 한 viewer에게 특정 문서 하나만 예외적으로 삭제
  권한을 준다(개인 실수를 그 사람이 직접 정리하게).
- 권한이 제한된 설계자(또는 그 신원을 대행하는 키)가 문서를 열었을 때,
  CLI/MCP가 그 제한을 놓치지 않고 알려줘야 한다.
- viewer 권한의 설계자가 웹 UI를 둘러볼 때, 어차피 눌러도 실패할
  생성/수정/삭제 버튼이 아예 안 보여서 "내가 뭘 할 수 있는지" 화면만
  보고 바로 알 수 있다.

### QA 체크리스트
- [x] owner/editor/viewer 기본 역할 경계(문서 읽기/쓰기/삭제) 왕복.
- [x] **CUD 버튼 권한별 숨김** - 비관리자 계정으로 웹 UI 전반(팀/그룹
  목록, 프로젝트 목록, 프로젝트 설정의 멤버 관리/문서 타입 관리/접근
  권한/git 저장소, 문서 에디터의 편집·삭제·상태전이·소스연결, 칸반
  분류·카드 생성, 문서/메시지 생성, 남의 코멘트 수정·삭제)를 클릭해
  권한 없는 CUD 버튼이 하나도 안 보이는지, 같은 화면을 실제 권한
  계정(owner/팀장/그룹 관리자/작성자 본인)으로 열면 전부 보이는지
  대조 확인 - 웹 UI 실제 클릭 + 서버 응답 필드(`isAdmin`/`myRole`/
  `canToggleHidden`/`perm`/`canManage`) 양쪽으로 실측.
- [x] 문서별 > 문서타입별 > 프로젝트 공통 오버라이드 우선순위, owner
  자기잠금 방지 - 실측.
- [x] 오버라이드 적용 시 `notices`가 CLI(`⚠` 줄)·MCP(별도 text 블록)
  양쪽에 실제로 뜨는지 - 실측.
- [x] API 키 스코프가 오버라이드보다 우선한다(비멤버 오버라이드로도
  스코프 밖 프로젝트는 못 들어감) - 실측(방어적 이중 장치까지 확인).
- [x] 비멤버에게 건 오버라이드가 실제로 그 비멤버에게 접근을 열어주는
  정상 케이스(오버라이드 자체의 존재 이유) - 실측.
- [x] 이 3단계 오버라이드 체인에 폴더 스코프는 없다 - 재검토 결과
  의도된 설계로 확정(폴더는 개인 소유 기능, 공유/제한 없음 - 6절
  참고).
- [x] 설계자별 접근 제한 프로젝트 횡단 조회 - 일반 설계자가 자기
  자신의 `GET /auth/me/access-overview`(CLI `access-overview`/MCP
  `access_overview`/웹 프로필의 "내 접근 제한")로 여러 프로젝트에
  걸린 오버라이드를 한 번에 확인(평소 그 프로젝트를 볼 수 있는지와
  무관하게 프로젝트 이름까지 노출) - 실측.
- [x] 관리자가 임의의 다른 설계자를 지목해 같은 정보를 조회(`GET
  /admin/users/:userId/access-overview`, CLI `user access-overview
  <userId>`, 웹 사용자 관리 화면의 "접근 제한 보기") - 비관리자는
  403 - 실측. `user_access_overview`는 `user_list`/
  `user_reset_password`와 같은 급의 신원 관리 동작이라 MCP엔 의도적으로
  없음(본인 조회용 `access_overview`는 MCP에 그대로 있음).

---

## 4. 문서 타입/상태 체계 (DocType/DocStatus/표준 6코드)

### 사용 시나리오
- 관리자가 프로젝트 전용 문서 분류(예: "회의록")를 새로 만들면 표준
  상태 6개가 자동으로 같이 만들어진다.
- 설계자가 문서 편집기에서 상태를 바꿀 때, 잘못된 상태(예: 이미
  보관된 문서를 초안으로)로 되돌리는 실수를 시스템이 구조적으로 막아야
  한다.
- 어느 상태에서 어느 상태로 옮길 수 있는지는 설계자가 미리 규제하지
  않는다(`#doctype-transition-ai-governed`) - `draft` 재진입 금지
  하나만 하드 규칙이고, 그 외엔 실질적인 작업자인 AI가 그때그때
  판단한다.
- "이 DocType이 어떤 상태들을 갖는가" 자체도 설계자가 개별로 관리할
  필요가 없다(`#doctype-status-auto-seed`) - 표준 6개는 항상 자동으로
  전부 갖춰진다.

**참고**: 문서 타입은 항상 그 프로젝트 자신에게만 정의된다 - 그룹/팀
단위로 획일화해 정하는 기능은 의도적으로 없다(설계자 확인 - 한때
`project→group→team` 상속 체인이 있었으나 완전히 제거됨, 아래 QA
체크리스트 참고).

### QA 체크리스트
- [x] 프로젝트 스코프 DocType 생성 - 실측.
- [x] **`#doctype-status-auto-seed`: DocType 생성 시(기본/커스텀 구분
  없이) 표준 상태 6개가 별도 호출 없이 자동으로 전부 만들어지는지,
  그 직후 바로 문서 생성이 성공하는지, 개별 추가/일괄 적용
  CLI 2개(`doctype-status-add`/`doctype-apply-standard-flow`)·MCP
  2개(`doctype_status_add`/`doctype_apply_standard_flow`)·UI 폼("상태
  추가"/"표준 상태 흐름 한 번에 적용" 버튼)이 전부 사라졌는지** - 전부
  실측.
- [x] "draft로는 어떤 상태에서도 못 돌아간다" 절대 규칙 - `draft`가
  `allowedNextStatuses()` 목록에 항상 빠지는지, 문서 상태 콤보박스에
  옵션 자체가 안 뜨는지 둘 다 실측.
- [x] **`#doctype-transition-ai-governed`: 상태 전이 CRUD(설계자가
  개별 전이를 정의/삭제하던 기능) 완전 제거 확인** - `DocStatusTransition`
  테이블 자체가 한 번도 존재한 적 없는 상태 조합(예: `pending`→
  `approved`)으로도 전이가 성공하는지, 제거된 CLI 3개/MCP 도구 3개가
  실제로 사라졌는지, `DocTypeManager.vue`에서 "전이" 섹션이 안 보이는지
  - 전부 실측.
- [x] 커스텀 라벨/지침 입력 없이 표준 6개 중 하나만 코드로 받는 제약 -
  실측.
- [x] DocType 자연어 지침(guideline) 생성/수정 + 문서 생성 화면 힌트
  노출 - 실측.
- [x] **그룹/팀 스코프 DocType(생성/상태/전이/표준흐름 + 상속 체인)
  완전 제거 확인** - 관련 CLI 12개가 전부 사라짐, MCP `doctype_*_team`/
  `doctype_*_group` 12개가 서버에서 사라짐, 팀/그룹 화면에서 "문서
  타입 관리" 버튼 자체가 안 보임, 제거 후에도 프로젝트 스코프 기본
  6종(SP/DC/PL/PD/RM/DS) 시딩이 회귀 없이 그대로 동작 - 전부 실측.
- [x] DocType 이름(code/label) 수정/삭제 - 기본 6종은 이름 수정
  시도 시 거부(삭제만 가능), 관리자가 직접 만든 타입은 이름 수정
  가능, 어느 쪽이든 그 타입으로 만든 문서가 남아있으면 삭제 거부(문서
  삭제 후 재시도하면 성공) - CLI/MCP/브라우저 전부 실측. 이름을 바꿔도
  이미 발급된 추적 코드 접두어는 안 바뀌는 것도 확인.

### 추가 개발 계획
현재 없음.

---

## 5. 문서 CRUD (생성/조회/저장/전이/링크/버전/삭제)

### 사용 시나리오
- 클로드가 새 설계 문서를 만들고, 다른 관련 문서로 링크를 걸고, 나중에
  그 문서를 참조하는 다른 문서들을 역추적한다.
- 설계자가 문서 하나를 여러 번 고치면서, 이전 버전과 지금 버전을
  비교하고 싶어 한다(변경 추적 뷰).
- 실수로 만든 문서를 삭제했는데, 그 문서에 달려 있던 코멘트/질의/링크가
  깨끗이 같이 정리돼야 한다.

### QA 체크리스트
- [x] 생성 → 조회 → 저장(리비전 자동 기록) → 상태 전이 → 링크 → 역참조
  왕복(여러 라운드 반복 검증).
- [x] 삭제 시 리비전/링크(양방향)/코멘트/질문+답변까지 cascade 정리,
  FK 에러 없음 - 실측.
- [x] 문서 저장/상태전이 응답에도 `createdBy`가 누락 없이 담기는지
  (이번 세션에서 직접 찾은 버그 수정 후 재확인) - 실측.
- [x] 읽기 모드 기본 + "편집" 클릭 시 Monaco 전환, `MarkdownBody`의
  XSS 새니타이즈(`<script>`/`onerror` 삽입 시도) - 실측.
- [x] 방금 만든 문서를 바로 그 자리에서 링크 대상으로 걸어도 실패 안
  하는지(Meilisearch write-through 경합 수정 후) - 실측.
- [x] 대량(수십~수백 건) 문서가 있는 프로젝트에서 목록/검색 응답
  속도 - 문서 200건짜리 프로젝트를 직접 만들어 실측. 응답 속도
  자체는 문제없음(전부 100ms 이내)이었지만, **CLI/MCP `docs
  list`(`GET /projects/:id/documents`, 페이지네이션 없는 "전체
  목록" 라우트)가 Meilisearch 기본 검색 limit(50)에 그대로 걸려
  51번째 문서부터 아무 경고 없이 빠지는 것을 발견·수정**
  (`#document-list-silent-cap` - `listDocuments()`에 명시적으로
  `limit: 1000`을 지정, 수정 후 200건 전부 반환 확인). 1000건을
  넘는 프로젝트는 여전히 범위 밖(잔여 한계로 기록, 실사용 규모를
  훨씬 웃돎).
- [x] **문서 우선순위(정수)** - `draft`에서 설정 시도 시 400, `review`
  전이 후 설정 시 200 + Meilisearch 경유 `GET`에도 값이 실제로 보이는지
  (인덱스 등록 누락 여부 확인), 값 갱신, `approved` 전이 후 재시도는
  400이면서 기존 값은 그대로 남는지, 쓰기 권한 없는 계정은 403 -
  전부 실측. CLI `priority-set`/MCP `document_priority_set` 왕복,
  웹 UI 배지·입력·저장 버튼 동작(범위 밖 상태에선 입력 숨김, 배지는
  유지) 확인. **실측 중 발견한 기존 버그**: `transition`/`save`/
  신규 우선순위 저장 응답을 `doc.value`에 통째로 덮어쓰면 그 응답에
  없는 `perm` 필드가 사라져 툴바가 런타임 예외로 깨지던 문제 - 병합
  방식으로 수정 후 재확인.

- [x] 문서 일괄 상태 전이/폴더 이동 - 서로 다른 타입 문서를 섞어
  일괄 전이(항목별 결과, 일부 실패해도 나머지 진행), 권한 없는
  계정으로 시도 시 그 문서만 실패, 폴더 일괄 이동/빼기 - CLI
  `transition-bulk`/MCP `document_transition_bulk`(일괄 폴더 이동은
  폴더가 AI 비가시 개념이라 웹 전용)/브라우저 전부 실측.
- [x] **문서 읽기 페이지에서 다른 문서로 이동 시 내용 갱신
  (`#document-nav-stale-content`)** - 사이드바 탐색기/문서 탭 트리
  양쪽에서 연속으로 다른 문서를 클릭해도 URL뿐 아니라 실제 본문·
  상태·우선순위가 매번 정확히 바뀌는지, 편집 모드로 열어둔 채
  다른 문서로 넘어가면 그 문서는 "보기" 모드로 정상 시작하는지(이전
  문서의 편집 상태가 새어 들어가지 않는지) 실측.
- [x] **연관 브랜치(`#pr-workflow-branch-scope`)** - 문서에
  `link-branch`로 브랜치를 연결 → 실제로 그 브랜치를 Gitea에서
  삭제해도 이 링크는 그대로 남아있는지(코드 관계도의 브랜치 스코프와
  반대 정책) 확인.

### 추가 개발 계획
현재 없음.

---

## 6. 문서 정리 폴더 (AI 비가시)

### 사용 시나리오
- 설계자가 문서가 많아진 프로젝트에서 폴더를 만들어 주제별로 정리한다.
- 폴더는 **만든 설계자 한 명만의 것**이다 - 공유/제한 기능이 없고,
  다른 설계자의 폴더는 존재 자체가 안 보인다(재검토 후 의도된 설계로
  확정 - `#folder-access-ui`, DESIGN-NOTES.md 참고).
- 클로드는 이 폴더 개념 자체를 몰라야 한다(CLI/MCP에 어떤 흔적도
  없어야 함).

### QA 체크리스트
- [x] 폴더 생성/이름변경(동명 금지)/삭제(비어있을 때만) 왕복.
- [x] 비어있지 않은 폴더 삭제 - `mode` 없이 시도하면 고정 에러로
  거부, `recursive`는 하위 폴더/문서 배치를 전부 삭제(문서 자신은
  보존), `promote`는 직속 하위 폴더/배치를 부모로 끌어올린 뒤 빈
  폴더만 삭제(목적지에 동명 폴더 있으면 거부) - 웹 UI에서
  비어있지-않음 에러 시 뜨는 재귀 삭제/끌어올리기 선택 버튼까지
  실측(폴더는 CLI/MCP 없음, 웹 전용).
- [x] 다른 설계자가 만든 폴더는 목록에 전혀 안 보임(존재 자체가
  숨겨짐, 접근 거부가 아님) - 실측.
- [x] 하위 폴더는 항상 자기 자신의 폴더 밑에만 만들 수 있음(다른
  설계자의 폴더 밑에는 생성 불가) - 실측.
- [x] 문서를 폴더로 이동 + 폴더 필터링(설계자마다 같은 문서를 각자
  다른 폴더에 독립적으로 배치 가능).
- [x] `document_list`/`document_get` 등 CLI/MCP 응답에 폴더 관련 필드가
  전혀 없음, CLI/MCP 어디에도 `folder` 명령/도구가 없음 - 실측.
- [x] **폴더+문서 통합 트리 뷰(`#document-folder-tree`)** - "문서" 탭이
  좌우 분할 대신 폴더+문서 하나의 재귀 트리로 통합됐는지, 임의 깊이
  (3단 이상) 하위 폴더가 전부 실제로 펼쳐지는지(구 2단계 제한 버그
  재발 안 함) 확인. 폴더 드래그로 다른 폴더의 자식이 되거나 같은
  부모 안에서 순서만 바뀌는 것 둘 다 새로고침 후에도 유지되는지,
  자기 하위 폴더 위로 드래그하면 거부되는지 확인. **문서도 드래그로
  폴더를 옮길 수 있는지**(최상단 "(미분류 문서)" 가상 노드 ↔ 실제
  폴더, 폴더 ↔ 폴더 전부) - trackingCode로 정확히 그 문서만 옮겨지고
  다른 문서/폴더는 안 건드리는지 확인(동명 문서/폴더가 여러 개 있는
  프로젝트에서는 반드시 trackingCode로 대조). 트리의 문서 행 클릭 시
  바로 읽기 페이지로 이동하는지. 문서 유형 필터 콤보 박스("+ 새 폴더"
  왼쪽)로 트리 전체(미분류 포함)의 문서 행이 걸러지는지(폴더/드래그
  순서 자체는 안 바뀌는지). 문서 읽기 페이지의 "폴더" 버튼 → 순수
  폴더 트리 다이얼로그(문서 없음) → 폴더 선택 후 "이동"으로 문서
  폴더가 바뀌는지. 폴더에 하위 폴더만 있고 문서가 없으면 "문서 없음"
  문구가 안 뜨는지. 트리에서 드래그 중 텍스트가 같이 선택되지
  않는지(`window.getSelection()` 빈 문자열). 폴더가 있던 문서를
  재귀 삭제해도 문서 자체는 그대로 열리는지(조건 확인).
- [x] **동시성(경합) - 폴더 맞바꿔 옮기기 순환 방지
  (`#folder-move-cycle-race`)** - 같은 설계자가 폴더 A를 B 밑으로,
  B를 A 밑으로 거의 동시에 옮기면 실제로 순환(트리에 A→B→A)이
  생기던 버그를 실제 재현(둘 다 성공 응답, DB에 순환 확정) → 설계자
  단위 프로세스 내 뮤텍스로 이동 요청을 직렬화하도록 수정 →
  재현 스크립트로 재실행해 먼저 실행된 쪽만 성공하고 나중 쪽은
  깔끔한 "하위 폴더 아래로는 옮길 수 없습니다" 에러로 거부되는지,
  순환이 더는 안 생기는지 확인. 서로 무관한 폴더 여러 개를 동시에
  옮기는 정상 시나리오가 데드락/과도한 지연 없이 그대로 되는지도
  회귀 확인 - 이번 세션에서 실측.

### 추가 개발 계획
현재 없음.

---

## 7. 질의/응답 (Q&A - AI가 묻고 설계자가 답한다)

### 사용 시나리오
- 클로드가 설계 결정이 필요한 지점에서 설계자에게 질문을 등록하고,
  판단에 참고한 문서 몇 개를 같이 태깅한다.
- 설계자가 웹에서 답을 남기면, 클로드는 다음 세션 시작 시 "확인 안
  한 답변이 있다"는 안내를 받고 챙겨서 처리해야 한다.
- 설계자가 실수로 아직 처리 안 된 질문에 또 답하려는 걸 시스템이
  막아야 한다.

### QA 체크리스트
- [x] open(AI 질의, 답변 대기) → pending(답변 완료, AI 확인 대기) →
  resolved(AI 확인 완료) 3단계 전이 - 실측.
- [x] 참고 문서 태깅(`--refs`)이 등록/답변/ack 전 구간에서 실제로
  유지되는지(이번 세션에서 발견한 "refs가 세 함수에서 빈 배열로
  하드코딩된" 버그 수정 후) - 실측.
- [x] `pending list`(=open+pending)에 미확인 답변 건수 `notices` 안내,
  ack 후 사라짐 - 실측.
- [x] 질문의 모든 open이 사라지면(유일한 다음 상태가 있을 때) 문서
  상태 자동 전이 - Phase 0 시절부터 검증, 최근 라운드는 회귀만 확인.
- [x] open/resolved 상태에서 ack 시도 시 거부 - 실측.
- [x] 질문 일괄 ack(`question-ack-bulk`/`question_ack_bulk`) - 여러
  pending 질의를 한 번에 확인 완료 처리(항목별 결과), 이미 처리됐거나
  존재하지 않는 질의를 섞어도 그 항목만 실패 - CLI/MCP 전부 실측
  (웹 UI엔 없음 - CLI 전용 시나리오).
- [x] 질문 철회(`question-withdraw`/`question_withdraw`) - 본인이
  낸 open 질문만 철회 가능(타인이 시도하면 거부, superAdmin은 우회),
  이미 답변된 질문은 철회 거부, 철회된 질문은 답변 시도 시 거부되고
  pending 목록에도 안 뜸 - CLI/MCP/브라우저 전부 실측.
- [x] 참고 문서가 삭제된 뒤에도 질의 자체는 안전하게 남아있는지 -
  문서 B의 질의에 문서 A를 참고 문서(`--refs`)로 달고 답변까지 단
  뒤 A를 삭제 → 질의/답변 본문은 그대로, `refs` 배열에서만 A가
  빠지는 것을 실측(스키마상 예상된 cascade 방향과 일치) - 이번
  세션에서 실측.
- [x] **동시성(경합) - answer/ack/withdraw** - 같은 open 질의에
  withdraw와 answer를 동시에 보내면 실제로 버그가 났었다(`#question-ack-race-fix`) -
  둘 다 200으로 성공 응답했지만 최종 상태는 withdraw가 조용히
  사라진 `pending`이었음. answer를 두 번 동시에 보내면 두 번째가
  Prisma 원본 예외를 그대로 노출. `db.question.updateMany({where:
  {id, status: 기대값}, ...})` 조건부 원자적 전이로 수정 - 이제
  동시 요청 중 정확히 하나만 성공하고 나머지는 깔끔한 도메인
  에러를 받는 것, pending 질의에 동시 ack 10회/bulk-ack 중복
  코드는 기존처럼 안전하게 동작하는 것, 서버가 크래시 없이
  응답하는 것(컨테이너 `Up` 유지, 처리 안 된 예외 스택트레이스
  없음) - 실측.

### 추가 개발 계획
현재 없음.

---

## 8. 코멘트 (설계자 전용 채널)

### 사용 시나리오
- 설계자 두 명이 문서 하나를 두고 웹에서 비공식적으로 의견을
  주고받는다(클로드가 볼 필요 없는 대화).
- 코멘트를 남긴 뒤 오타를 발견하면 본인이 직접 고치거나, 잘못 단
  코멘트는 지운다.

### QA 체크리스트
- [x] 코멘트 등록/조회/해결(resolve) 웹 UI 왕복.
- [x] 코멘트 수정/삭제(`PUT`/`DELETE /api/comments/:id`) - 작성자
  본인(또는 superAdmin)만 가능 - 실측(`#question-withdraw` 라운드
  조사 중 이미 구현·배포돼 있었음을 재확인, HTTP 왕복으로 검증).
- [x] CLI(`docs comment` 자체가 명령으로 없음)/MCP(`comment_*` 도구
  없음) 완전 배제, REST API/웹 UI는 정상 동작(회귀 없음) - 이번
  세션의 CLI/MCP 대칭성 감사에서 재확인.

### 추가 개발 계획
현재 없음.

---

## 9. 메시징(대기/처리중/기록/장애 복구) + 실시간 전파(EMQX)

### 사용 시나리오
- 설계자가 클로드에게 "이 작업 끝나면 알려달라"는 메시지를 남기고,
  클로드는 `message wait`로 블로킹 대기하다가 응답한다.
- 시스템이 다운됐다가 복구된 뒤, 클로드가 "그 사이에 무슨 지시가
  있었는지" `message recent`로 부작용 없이 훑어본다.
- 웹 UI를 보고 있는 두 설계자가, 한쪽이 메시지를 보내면 새로고침 없이
  바로 반대쪽 화면에 뜨는 걸 기대한다.

### QA 체크리스트
- [x] 대기(deliveredAt null)/기록 분리, CLI/MCP 호출이 자동으로
  대기→기록 전환(웹 UI는 전환 안 시킴) - 실측.
- [x] `message recent`가 상태를 절대 바꾸지 않는지(반복 호출 전후
  `message list --status pending` 결과 동일) - 실측.
- [x] 문서 에디터의 "메시지로 지시" - 추적 코드 자동 병기, 클릭 시
  다이얼로그 - 이전 라운드 실측.
- [x] JWT 기반 EMQX 인증 + Member 기준 topic ACL(Phase 4에서 raw MQTT
  클라이언트로 직접 검증 - 다른 프로젝트 topic 구독 거부, 위조 JWT
  거부).
- [x] `message wait`가 실제로 "새 메시지 도착 즉시 반환"하는지와
  "타임아웃 시간만큼 정확히 대기 후 빈 응답"하는지 - 서버 쪽 단일
  호출은 최대 10초로 제한되고 CLI/MCP가 그 짧은 폴을 반복 호출(폴링)
  하는 구조로 바뀐 뒤 재실측: `timeout=1` 직접 호출 → 약 1초 후
  정상 타임아웃, `timeout=999`(상한 초과) 직접 호출 → 실제로는 약
  10초 만에 응답(클램프 확인), CLI/MCP 양쪽 다 대기 도중 보낸
  메시지를 다음 폴에서(또는 첫 폴 안에서) 즉시 잡아내는지 확인.
- [x] **`message wait`의 CLI/MCP 직접 MQTT 구독 전환(`#message-wait-mqtt-direct`)** -
  전용 MQTT 계정(`GET /api/auth/me/mqtt-credentials`) 왕복 멱등성
  확인, CLI/MCP 둘 다 메시지 발행 즉시(수 초 이내, MCP는 약 2ms) 잡히고
  `deliveredAt`이 정상적으로 찍히는지 실측. `PUBLIC_EMQX_MQTT_URL`을
  비웠을 때 기존 HTTP 폴링으로 정상 폴백하는지, 복원 후 다시 직접
  구독 경로로 동작하는지 확인. 웹 UI의 JWT 기반 실시간 갱신은 이
  변경과 무관하게 회귀 없이 그대로 동작(브라우저로 실제 메시지
  화면을 열어두고 CLI로 메시지를 보내 새로고침 없이 뜨는지 확인).
- [x] 메시지 수정/삭제(`message edit|delete`/`message_edit`/
  `message_delete`) - 본인이 보낸 메시지만(또는 superAdmin 우회)
  가능, 다른 사람 시도 시 거부 - CLI/MCP/브라우저 전부 실측.
  **회귀 확인**: `message wait`를 걸어둔 채 무관한 메시지를 편집해도
  그 대기 호출이 편집을 가짜 새 메시지로 받지 않고 정상 타임아웃
  되는지 확인(편집/삭제 이벤트는 messages가 아니라 changes 토픽으로
  나감).
- [x] **대기/처리중/기록 3단계 분류(`#message-processing-status`)** -
  새 메시지는 항상 대기(`ackedAt` 없음)로 시작하고, 단순 조회
  (`message list`/`message_list`/`message wait`)만으로는 상태가
  안 바뀌는지 확인. `message ack`/`message_ack`로 대기→처리중,
  `message complete`/`message_complete`로 처리중→기록 전이 확인 -
  ack 없이 바로 complete를 불러도 `ackedAt`까지 자동으로 채워지는지,
  이미 처리중/기록인 메시지에 다시 ack/complete를 불러도 에러 없이
  그대로 반환되는지(idempotent). ack/complete는 보낸 사람이 아닌
  다른 프로젝트 멤버도 가능한지(소유권이 아니라 멤버십만 확인).
  **대기 상태 메시지는 수정이 거부되고(400 + 고정 에러 문구) 삭제는
  그대로 되는지** REST 직접 호출로 확인. 웹 UI에서 대기 탭엔 "처리
  시작"만(수정 버튼 자체가 안 보임), 처리중 탭엔 "완료"+수정+삭제,
  기록 탭엔 수정+삭제만 뜨는지 확인.
- [x] **동시성(경합) - ack/complete가 서버를 충돌내지 않는지** -
  같은 메시지에 ack 10개 동시 호출, complete 10개 동시 호출,
  ack+complete 혼합 10개 동시 호출, `markDelivered=true` 목록
  조회와 ack를 동시 반복, 존재하지 않는 id로 ack/complete - 전부
  200 또는 깔끔한 도메인 에러로 응답하고 컨테이너가 크래시 없이
  계속 `Up` 상태인 것을 실측(`ackMessage`/`completeMessage`는
  "이미 처리됐으면 그대로 반환"하는 idempotent 설계라 애초에 경합
  안전했음 - 질의 ack/answer/withdraw 쪽은 실제 버그가 있었다,
  `#question-ack-race-fix` 참고).

### 추가 개발 계획
현재 없음.

---

## 10. 검색/인덱싱 (Meilisearch write-through)

### 사용 시나리오
- 클로드가 막 만든 문서를 그 자리에서 바로 검색/링크/조회할 때 항상
  최신 상태를 봐야 한다(캐시 지연으로 인한 유령 실패가 없어야 함).
- 설계자가 프로젝트 전체를 대상으로 키워드 검색을 한다.

### QA 체크리스트
- [x] 문서 생성/수정/삭제가 검색 결과에 지연 없이 반영되는지(Phase 0
  최초 검증 + 이번 세션 마이그레이션 점검에서 근본 원인을 찾아
  `.waitTask()`로 수정 후 재검증) - 실측.
- [x] 쓰기 직후 바로 링크 생성 등 후속 쓰기가 그 문서를 대상으로 해도
  실패하지 않는지 - 실측(수정 전/후 대조까지).
- [x] Meilisearch 컨테이너가 죽어 있을 때 시스템 전체가 어떻게
  degrade되는지 - 실측(`docker compose kill meilisearch`로 의도적
  장애 주입): 조회는 즉시 503 + 명확한 한국어 메시지(내부 호스트명
  노출 없음, 문서 없음(404)과 혼동 안 됨), 서버는 안 죽고 개별 요청만
  실패, 새 문서 생성은 DB 커밋 성공 + 색인 반영은 큐에 적재돼 장애
  해소 후 30초 워커가 자동 재처리(admin `search-queue status`/
  `drain`으로 상태 확인/수동 드레인도 가능).
- [x] **기존 문서 수정/삭제/전이도 장애 중 큐로 보호되는지
  (`#document-write-gate-bypass-search`)** - 사전 권한 확인을 DB
  직접 조회(`getDocumentAccessInfo`)로 바꾼 뒤 재실측: 장애 중 같은
  문서에 PUT(본문 수정)/전이/우선순위 변경을 연속 시도해 전부 200
  성공(수정 전엔 503), 검색 동기화 큐에 정확히 적재(같은 트래킹코드라
  1건으로 중복 제거), DELETE도 200 성공 후 큐에 반영. 문서 **내용
  조회**(`GET /documents/:trackingCode`)는 의도적으로 여전히 503
  (회귀 아님 - 조회는 검색 엔진을 거치는 게 원칙). 장애 해소 후
  드레인 워커가 자동으로 큐를 비우고 실제 삭제 상태가 검색에
  반영(404)되는 것까지 확인.

---

## 11. git 저장소 연동 (3가지 방식) + 동기화(발행)

### 사용 시나리오
- 설계자가 새 프로젝트에 빈 Gitea 저장소를 만들어 소스 코드를 관리
  시키기 시작한다.
- 설계자가 기존 GitHub 저장소의 히스토리를 통째로 Gitea로 옮겨와
  이때부터 Gitea가 유일한 저장소가 되게 한다.
- 설계자가 외부 GitHub 저장소는 계속 권위 있는 원본으로 두고, 이
  시스템은 그 안에서 벌어진 변경을 "동기화 제안"으로 미리 보거나,
  깨끗하게 반영 가능하면 "동기화" 버튼으로 실제로 push한다.
- 외부 저장소가 그 사이에 다른 사람 손으로 앞서가 있어 즉시 반영이
  안 되면, AI가 대신 확인해 처리하고 완료를 보고하길 원한다.
- git 저장소 관련 기능은 그 프로젝트를 실제로 책임지는 owner만
  건드릴 수 있어야 한다(viewer/editor가 실수로 저장소 연결을 바꾸는
  사고 방지).

### QA 체크리스트
- [x] 옵션 1(새 저장소 생성) - Gitea API로 실제 생성 대조(Phase 2).
- [x] **이번 세션에서 새로 발견·수정**: `gitea.createRepo()`가
  `auto_init: true`로 Gitea 저장소를 만들어, 호출부(`linkSelfHostedRepo`)
  자신의 주석이 말하는 "빈 저장소" 의도와 실제로 어긋나 있었음을
  README.md "도입 시나리오별 안내" 절차를 실제로 리허설하며 발견 -
  Gitea가 스스로 만든 기본 브랜치(예: `main`, README 자동 커밋 포함)와
  설계자가 로컬에서 `git push -u origin <다른 이름의 브랜치>`로 만드는
  브랜치가 서로 무관한 별개 브랜치로 남아, 그 뒤 `template deploy`가
  (항상 저장소의 `default_branch`에 커밋하므로) 커밋한 CLAUDE.md/
  SKILL.md가 설계자의 `git pull`에는 전혀 안 보이는 문제로 실측
  재현됐다(`auto_init:false`로 수정 후 동일 절차 재실행 - 링크 직후
  `empty:true` 확인, 설계자의 첫 push로 `default_branch`가 그 브랜치
  이름으로 정확히 바뀌는지, 그 뒤 `template deploy`+`git pull`이
  fast-forward로 CLAUDE.md/SKILL.md를 정확히 받는지까지 전부 재확인).
- [x] **이번 세션에서 새로 발견·수정**: "웹 설치 마법사 폐지"
  (`#gitea-nginx-lockdown`)로 도입한 CLI 기반 Gitea 관리자 부트스트랩
  (`gitea admin user create`/`generate-access-token`)이, 실제로 **한
  번도 웹 설치를 거친 적 없는 완전히 새 Gitea volume**에서는
  `INSTALL_LOCK=false`인 채로 부팅돼 두 CLI 명령 다 "Unable to load
  config file for a installed Gitea instance"로 거부되는 것을 실제
  신규 설치(별도 디렉터리, 새 volume)를 구성하며 발견 - 기존 dev
  스택의 Gitea는 그 기능이 생기기 전에 웹 마법사로 이미 설치돼 있던
  volume이라 이 문제를 계속 못 보고 넘어갔었다.
  `docker-compose.yml`의 `gitea` 서비스에
  `GITEA__security__INSTALL_LOCK: "true"`를 추가해 완전히 새 volume도
  처음부터 CLI만으로 부트스트랩되도록 수정 - 빈 volume으로 재현 후
  수정, 재현 절차 그대로 재실행해 `admin user create`→
  `generate-access-token`→`.env` 반영→`git link`→실제 org/저장소
  생성까지 왕복 확인.
- [x] **이번 세션에서 새로 발견·수정(설치 관련, 이 시나리오와 무관하지
  않음)**: 이 저장소에 `.gitattributes`가 없어, `core.autocrlf=true`인
  머신에서 새로 `git clone`하면 `backend/docker-entrypoint.sh`가
  CRLF로 체크아웃돼 backend 컨테이너 기동이 셔뱅 파싱 실패로 막히는
  것을 발견(실제 신규 설치 클론에서 재현) - `*.sh text eol=lf`를 추가해
  수정. 같은 신규 설치 과정에서 Docker Compose가 폴더 이름만으로
  프로젝트를 식별해 서로 다른 경로의 두 설치가 같은 이름의 볼륨(DB
  등)을 공유해버리는 것도 발견 - `COMPOSE_PROJECT_NAME`으로 분리하도록
  README.md/`.env.example`에 안내 추가(코드 변경 없음, 배포 topology
  문제).
- [x] 옵션 2(외부 저장소 이주) - 실제 커밋 히스토리가 그대로 들어오는지
  Gitea API로 대조.
- [x] 옵션 3(외부 연동) - 미러+작업 저장소 둘 다 생성, 소스
  브라우저/`git log`가 작업 저장소 기준으로 정상 동작.
- [x] 비공개 저장소 인증 필요 케이스 - 자격증명 없이 시도 시
  `401 git_auth_required`, 웹에서 그 자리에서 자격증명 입력 후 자동
  재시도.
- [x] `git blame` 미지원의 명확한 에러(Gitea API 자체 한계, 실측으로
  확인).
- [x] **동기화 제안(`git sync-status`/`git sync-proposal`) 재실측** -
  완료. 새 스크래치 프로젝트("QA Sync Test")를 공개 GitHub 저장소
  (`octocat/Hello-World`)에 옵션 3(외부 연동)으로 연결 → Gitea에
  `-mirror`(mirror:true)/`-work`(mirror:false) 두 저장소가 실제로
  생겼는지 대조(각각 21456/21445바이트로 실제 클론된 내용) → 소스
  에디터로 `README` 파일을 수정(work 저장소에 커밋) → `POST .../git/
  sync-status`로 트리거(`scheduled` 응답) → `GET`으로 폴링해
  `status:"ready", changed:["README"]`를 확인 → `GET .../git/
  sync-proposal`이 방금 수정한 실제 내용을 그대로 반환하는지 확인.
  API 키 스코프도 같이 확인 - 다른 프로젝트 전용 키로 이 두 라우트를
  호출하면 둘 다 403(스코프 게이트가 자동 적용됨을 실측으로 확인).
- [x] **실제 Gitea 웹훅 왕복 테스트**(전체 기능 회귀 QA 라운드에서
  완료) - 로컬 저장소를 clone해 `release/1.0`(트리거 패턴
  `release/*`와 매칭)과 `main`(안 매칭) 두 브랜치에 각각 push - 매칭
  브랜치만 대기열에 실제로 쌓이고, 안 매칭 브랜치는 큐에 새 항목이
  안 생기는지 실측. `hook ack`/`hook done` 전이도 확인.
- [x] **git 저장소 기능 owner 전용화** - `POST/GET .../git/sync-status`,
  `GET .../git/sync-proposal`이 과거엔 `viewer`도 호출 가능했던 회귀를
  발견·수정, viewer 계정으로 재현 시 403, owner 계정은 통과하는지
  실측. 웹 UI에서도 owner 아닌 멤버에겐 "git 저장소" 섹션 전체가 안
  보이는지 확인.
- [ ] **동기화(발행) - 실제 GitHub/GitLab 테스트 저장소로 왕복** -
  Gitea Push Mirror 등록 → 깨끗하게 fast-forward 가능한 상태에서
  "동기화" 클릭 → 외부 저장소에 실제로 커밋이 반영되는지 GitHub/GitLab
  쪽에서 직접 확인. 쓰기 권한 없는 자격증명으로 시도 시 명확한 실패로
  대기열에 올라가는지, 외부 저장소를 일부러 앞서가게 만든 뒤 시도해
  같은 경로로 대기열에 올라가는지, `docs git publish-queue-done` 보고
  후 버튼이 재활성화되는지 - 코드/설계 검토는 마쳤지만 아직 이 문서
  형식의 실측 왕복은 안 함(우선 재개 후보).
- [x] **외부 연동 해제(`git unlink`/`git_unlink`)** - 실제 공개
  저장소(`octocat/Hello-World`)로 외부 연동 후 작업 저장소에 마커
  파일 커밋 → 해제 → `self_hosted`로 전환되고 마커 파일이 그대로
  살아있는지(히스토리 보존), 해제 후 새 커밋도 정상 동작하는지
  확인. 자체 호스팅 프로젝트는 해제 시도 자체가 명확히 거부되는지,
  연결 안 된 프로젝트는 명확한 에러가 오는지 확인. 웹 UI 버튼 클릭
  왕복(confirm 취소/승인 둘 다)까지 실측.
- [x] **로컬/사설 서버 배포 시 외부 웹훅 수동 설정 안내
  (`#external-webhook-manual-instructions`)** - 백엔드는 이미
  `link-external`이 웹훅 자동 등록에 실패하면 `manualWebhookInstructions`
  (URL+secret)를 응답에 실어 보냈지만, `GitRepoPanel.vue`가 그
  필드를 타입에 아예 선언 안 해서 화면에 조용히 버려지고 있었음을
  발견·수정 - 자동 등록 실패 시 웹 UI에 Payload URL/Secret/
  Content-Type/이벤트(push)를 보여주는 안내 카드가 뜬다.
- [x] **웹훅 수동 설정 카드 - 접기/펼치기 + 실제 수신 전까지 영구
  노출(`#webhook-instructions-persistent-card`, 설계자 지시)** -
  처음엔 "확인함" 버튼으로 영구히 닫히는 1회성 안내였는데, 설계자
  지시로 "웹훅이 실제로 호출을 받기 전까지는 접을 수 있는 카드로
  계속 노출"하도록 재설계 - `ProjectGitRepo`에 `webhookAutoRegistered`/
  `webhookUrl`/`webhookFirstReceivedAt`을 영속화하고, 새
  `GET .../git/webhook-instructions`(owner 전용)로 새로고침·재방문
  때마다 다시 조회, 외부 웹훅 수신 라우트가 처음 호출될 때
  `webhookFirstReceivedAt`을 찍어 그 뒤로는 카드 자체가 안 뜨게 함.
  실제 GitHub 웹훅 서명(`X-Hub-Signature-256`, HMAC-SHA256)을 그대로
  재현한 스크립트로 실측: (1) 자격증명 없이 연동해 카드가 뜨는지,
  (2) 헤더 클릭으로 접기/펼치기가 되는지, (3) 새로고침해도 같은
  URL/secret으로 카드가 그대로 남아있는지(핵심 - 예전엔 1회성이라
  안 됐음), (4) 올바른 서명으로 실제 웹훅 호출을 한 번 흉내내면
  `200 {"ok":true}` 응답과 함께 그 다음 새로고침부터 카드가 완전히
  사라지는지 확인. 콘솔에 새 에러 없음.
- [x] **GitHub OAuth 연동 + self_hosted↔external 승격 + 자격증명
  오류 자동 강등(`#github-oauth-repo-link`)** - OAuth 미설정 상태에서
  `GET .../git/oauth/github/configured`가 `false`를 반환하고 "GitHub로
  로그인" 버튼이 웹 UI에 안 보이는지 확인(fail-soft). `promote-to-external`
  라우트의 입력 검증(잘못된 provider/repoUrl 누락/gitCredentialId
  누락/존재하지 않는 자격증명) 4가지 전부 400 확인. **핵심 통합
  테스트**: 무효한 GitHub PAT로 공개 저장소(`octocat/Hello-World`)에
  `promote-to-external` 호출 → 미러 생성+work 저장소 승격까지 성공한
  뒤 곧바로 실행되는 발행 사전 검증이 실제 GitHub API(`GET /user` →
  401)로 무효를 확인해 자동으로 `self_hosted`로 되돌리고(재조회로
  `provider`/`gitCredentialId` 확인) 프로젝트 메시지함에 안내가 남는
  것까지 실측 - 자격증명 오류 자동 강등이 실제 GitHub API를 상대로
  end-to-end 동작함을 확인. 저장소 목록 조회(`GET /api/credentials/
  :id/github/repos`)도 무효 토큰의 401이 명확한 400으로 전달되는지,
  소유자 아닌/존재하지 않는 credentialId가 거부되는지 확인. OAuth
  `start`/`callback`은 미설정 상태에서 각각 명확한 에러로 안전하게
  종료되는지 확인. **실제 GitHub OAuth App으로 하는 팝업 로그인
  왕복(승인 → 저장소 선택 다이얼로그 → 연동) 자체는 이번 세션에 그런
  앱이 없어 미검증** - 설계자가 실제 OAuth App을 등록하면 별도로
  한 번 더 확인 필요(재개 후보로 남김).
- [x] **저장소 관리 탭 - 브랜치 목록/열람 + PR 생성/머지
  (`#repo-management-tab`)** - Gitea REST API를 직접 호출해 실제
  브랜치와 그 위의 커밋을 만든 뒤(이 앱엔 브랜치 생성 UI가 없어
  설계자가 실제로 git push하는 상황을 재현), `GET .../git/branches`가
  실제 브랜치/commit sha/시각을 정확히 반환하는지, 저장소 관리 탭의
  "탐색" 버튼이 `/source?ref=<브랜치>`로 이동해 **기본 브랜치엔 없는
  파일까지 실제로 보이고 기본 브랜치로 돌아가면 다시 안 보이는지**
  실측. PR 생성 시 `authorUsername`이 실제로 그 설계자의 Gitea 계정
  명의로 찍히는지(admin 계정/실제 초대한 별도 editor 계정 둘 다) 확인 →
  owner로 머지 → PR이 "머지됨"으로 바뀌고 **main 브랜치에 실제로 병합된
  파일이 반영된 것을 재조회로 확인**(진짜 머지 커밋) → editor 계정으로
  같은 PR 머지 시도 시 서버가 실제로 403으로 거부하고 그 계정의 웹
  UI에서는 "머지" 버튼 자체가 안 보이는지(admin 계정 화면엔 보임과
  대비) 확인 - owner 전용 제약이 서버/프론트 양쪽에서 일관되게
  강제됨을 확인. 테스트 프로젝트는 검증 후 삭제.
- [x] **PR 워크플로우 확장 - 전용 상세 페이지/Reject·Close·Reopen/
  수동 병합 완료(`#pr-workflow-branch-scope`)** - 실제 로컬 clone으로
  PR 4개 실전 왕복(자동 머지 성공/거부 후 재오픈+재머지/선택 없이
  Close 시 자동 거부 처리/실제 충돌로 자동 머지 실패시켜 수동 병합
  완료까지). 상세 내용은 DESIGN-NOTES.md 해당 라운드 참고.
- [x] **Gitea 프로젝트별 네임스페이스(`#gitea-per-project-namespace`)** -
  실제로 새 프로젝트를 만들어 `git link` → 프로젝트 전용 org(`proj-
  <id>`)에 `repo` 저장소가 생성되는지 확인 → 그 org로 실제 clone/push
  → 시스템 웹훅이 push/delete 둘 다 org+저장소 이름 기반으로 정확히
  프로젝트를 매칭하는지(Meilisearch 소스 색인 반영, 브랜치 삭제 시
  관계도 cascade) 확인. 기존(구 공유 org `cnwk-projects`) 스킴으로
  만들어진 실제 프로젝트 11건(self_hosted 9건, external_linked 1건,
  고아 DB행 1건)에 `migrate-gitea-namespaces`를 실행 - transfer API로
  9건 성공, 이미 이전된 신규 프로젝트 1건은 멱등하게 스킵, Gitea에서
  저장소가 이미 삭제된 고아 행 1건만 명확한 실패로 보고되는지 확인 →
  `verify-gitea-namespaces`로 전부 재확인 → 이전된 프로젝트의 기존
  PR 이력(디스포지션 포함)이 그대로 남아있는지 확인. `PUBLIC_GITEA_URL`
  설정 후 `GET .../git/repo`의 `repoUrl`이 그 값 기준으로 즉시
  재계산되는지, **미설정 시 DB 저장값으로 정확히 폴백하는지도**(잠시
  값을 지우고 backend 재기동 → 확인 → 원복) 확인. **후속 QA 라운드
  추가 검증**: `git link-external`로 mirror+work 두 저장소가 새 org
  아래 실제로 생성되는지, `git unlink`가 work를 "repo"로 이름만 바꿔
  self_hosted로 전환하면서 커밋 히스토리(`git log`)가 그대로 살아있고
  mirror는 정리되는지, 프로젝트 멤버 추가/제거 시 Gitea 협업자 목록이
  새 org의 저장소 기준으로 정확히 동기화되는지, 프로젝트 삭제 시
  저장소뿐 아니라 그 프로젝트 전용 org 자체도 Gitea에서 사라지는지
  (`GET /orgs/{org}` 404) 전부 실제로 만들고 지워보며 확인.
- [x] **nginx 리버스 프록시 + Gitea 웹 UI 비노출(`#gitea-nginx-lockdown`)** -
  실제 스택 기동 후 `docker compose ps`로 gitea/backend 포트가 호스트에
  전혀 안 열려있고 nginx(80)만 열려있는지 확인 → gitea(3001)/backend
  (8760) 직접 접속이 연결 자체가 거부되는지 확인 → nginx(80)를 통한
  `.git` 경로로 실제 `git clone`/`push`가 성공하는지, 동시에 같은
  origin의 비-`.git` 저장소 경로가 Gitea 웹 UI가 아니라 이 앱 자신의
  화면으로 응답하는지 확인 → `docker exec ... gitea admin user
  create`/`generate-access-token` CLI로 관리자 계정/PAT 발급이 실제로
  되는지 확인(웹 설치 마법사 없이). **후속 QA 라운드 추가 검증**:
  `gitea admin user create`/`generate-access-token`의 실제 `--help`
  출력이 README에 적힌 플래그와 정확히 일치하는지 재확인, nginx
  기본 body 크기 제한(1m)보다 큰 5MB 파일 push가 `client_max_body_size
  0` 설정 덕에 실제로 성공하는지 확인(둘 다 통과) - **이 과정에서
  실수 하나 발견**: 로컬 테스트 `.env`의 `PUBLIC_GITEA_URL`이 nginx
  도입 이전의 직접 노출 Gitea 포트(`:3001`)를 그대로 가리키고
  있었던 것 - 그 상태면 designer에게 보이는 clone 주소가 접속 불가
  포트를 계속 가리키게 됨을 재현 확인 후 nginx 경유 주소로 정정,
  README의 "기존 설치 업그레이드" 절에 동일한 실수를 막는 경고 추가.
- [x] **관계도 초기화 + 추적코드 선택기(`#relations-reset-and-picker`)** -
  웹 UI에서 "관계도 초기화" 버튼 → 확인 다이얼로그(모든 브랜치/브랜치
  없음/실제 브랜치명 선택지) → 삭제 후 해당 범위만 실제로 지워지는지
  CLI로 재조회해 확인. "새 관계 추가" 폼에서 텍스트 입력 대신 "추적코드
  선택" 버튼이 문서 선택기(EntityPickerDialog)를 열고, 검색+선택+칩
  표시/제거가 정상 동작하며 저장 결과에 정확한 추적코드가 반영되는지
  확인.

---

## 12. push 훅 자동화 (웹훅 → 대기열)

### 사용 시나리오
- 설계자가 "main 브랜치에 push되면 배포 체크리스트를 클로드가 처리
  하게" 프롬프트 트리거를 등록해둔다.
- 다른 설계자가 실제로 push하면, 다음에 클로드 세션이 그 프로젝트를
  열 때 대기열에 쌓인 항목을 확인하고 처리한다.
- 클로드가 대기열 항목을 처리하다가, 실수로 다른 프로젝트의 대기열
  항목 id를 잘못 넘겨도 그 프로젝트 경계를 못 넘어야 한다.

### QA 체크리스트
- [x] 웹훅 서명 검증(Gitea/GitHub/GitLab 3종 provider 정규화) - 단위
  구현 확인.
- [x] `PushHookPrompt` CRUD(owner 전용) + 대기열 조회(viewer)/처리
  (editor) 인가 수준 - 라우트 코드 확인.
- [x] **이번 세션에서 새로 발견·수정**: `push-hook-queue/:id/ack`·
  `/done`이 경로의 `:projectId`와 무관하게 `:id`만으로 큐 항목을
  찾아, 다른 프로젝트의 editor가 남의 프로젝트 대기열 항목을
  ack/done 처리할 수 있었던 IDOR 취약점을 `pushHookPrompt.projectId`
  대조로 수정(코드 수정 완료, **아직 실제 웹훅 왕복으로 재검증
  안 됨** - 아래 참고).
- [x] **실제 Gitea 컨테이너로 웹훅이 도착 → `PushHookQueueEntry` 생성
  → ack → done 전체 왕복** - 실측 완료. QA Project(자체 호스팅 Gitea
  저장소, 웹훅 이미 자동 등록돼 있음)에 트리거 브랜치 없음(전체 매칭)
  프롬프트를 등록한 뒤, Gitea Contents API로 실제 커밋을 만들어(백엔드를
  거치지 않고 Gitea가 직접 웹훅을 쏨) `PushHookQueueEntry`가 그 커밋
  sha로 정확히 생성되는지, 이어서 `ack`→`done` 상태 전이까지 API로
  확인.
- [x] 방금 고친 IDOR 수정이 실제로 막는지 - 실측 완료. QA Project의
  대기열 항목 id를 admin이 owner인 **다른** 프로젝트(QA Project B)의
  `:projectId` 경로로 ack 시도 → `400 "큐 항목을 찾을 수 없습니다"`로
  거부되고 항목 상태도 그대로 `pending`으로 안 바뀌는지 확인(같은
  사용자가 두 프로젝트 다 owner라 role 검사만으로는 안 걸리고, 오직
  `pushHookPrompt.projectId` 대조가 막는 케이스라는 걸 정확히 겨냥).
- [x] `triggerBranch` 지정/미지정(전체 브랜치) 매칭 분기 - 실측 완료.
  같은 push에 대해 `triggerBranch: null`(전체) 프롬프트는 큐 항목이
  생기고, `triggerBranch: "release/x"`(main과 불일치) 프롬프트는 생기지
  않는지 한 번의 실제 웹훅으로 동시에 대조 확인.
- [x] API 키 스코프(프로젝트 키/팀 키)가 이 라우트들에도 제대로
  걸리는지 - 실측 완료. QA Project B 전용 프로젝트 키로 QA Project의
  큐 조회/프롬프트 생성 시도 → 둘 다 403(`getMemberRole`이 스코프
  밖이라 null 반환 → `requireProjectRole`이 거부) - 같은 키로 자기
  프로젝트(B) 조회는 200으로 정상 동작해 스코프 게이트가 "이 라우트만
  막힘"이 아니라 "이 라우트도 자동으로 걸림"임을 확인.
- [x] **`PushHookPrompt` 부분 갱신(`hook update`/`hook_update`,
  `#hook-prompt-update`)** - 프롬프트 내용만 교체(브랜치 유지),
  브랜치를 빈 문자열로 교체(제한 해제 - `null`) 각각 실측. 다른
  프로젝트 id로 시도 시 소유 불일치 거부, 빈 프롬프트 거부 확인.
  CLI에서 `--branch ""`(공백)는 commander가 값 누락으로 오인하니
  `--branch=""`(등호)로 붙여 써야 한다는 함정 확인.
- [x] **브랜치 조건 glob 패턴(`#hook-branch-pattern`)** - 실제 Gitea
  저장소로 실측: `release/*` 프롬프트 등록 → `release/1.0` 브랜치에
  실제 커밋(Contents API로 백엔드 우회, 진짜 push 웹훅 유발) → 매칭돼
  큐 항목 생성 확인. `release/2.0/hotfix`(세그먼트 2개) 브랜치 커밋 →
  매칭 안 되고 큐 항목 없음 확인(세그먼트 경계가 의도대로 `/`를 안
  넘는지의 핵심 증거). 리터럴 `main` 프롬프트는 이번 변경과 무관하게
  회귀 없이 그대로 동작.
- [x] **대기열 만료/자동 정리(`#hook-queue-ttl`)** - 30일을 실제로
  기다릴 수 없어 `triggeredAt`을 직접 31일 전으로 되돌려 재현:
  `acknowledged` 처리한 오래된 항목은 그대로 유지, `pending`인
  오래된 항목만 `expired`로 전이, 최근 `pending` 항목은 안 바뀌는지
  확인. `hook queue --status expired` 필터가 코드 변경 없이 바로
  동작하는지 확인.

---

## 13. 템플릿 관리 (CLAUDE.md/SKILL.md) + 배포

### 사용 시나리오
- 관리자가 설치 전역 기본 CLAUDE.md를 조직 컨벤션에 맞게 고친다.
- 팀장이 팀 전용 SKILL.md 오버라이드를 걸어, 그 팀 프로젝트에서만
  다른 안내를 준다.
- 설계자가 해석된(override 체인 적용된) 최종 템플릿을 실제 프로젝트
  git 저장소 루트에 커밋해 배포한다.

### QA 체크리스트
- [x] project→group→team→전역 기본값 override 우선순위 왕복(Phase 1).
- [x] `template deploy`가 실제 Gitea 저장소에 커밋되는지 Gitea API로
  대조(Phase 2).
- [x] `template deploy`가 항상 저장소의 `default_branch`에 커밋한다는
  전제가 self_hosted `git link`(옵션 1) 직후에도 깨지지 않는지 -
  §11의 `auto_init` 버그 수정 후 재확인 완료(수정 전엔 Gitea가 미리
  만든 브랜치와 설계자의 push가 갈라져 있어 이 전제가 깨져 있었음).
- [x] **이번 세션에서 새로 발견·수정**: `PUT /api/templates`가
  스코프(teamId/projectGroupId/projectId) 중 아무것도 안 넘기면
  "설치 전역 기본값" 수정인데, 이 경로에 API 키 스코프 확인이 전혀
  없어서 **프로젝트 하나로 좁힌 키가 설치 전역 CLAUDE.md/SKILL.md를
  덮어쓸 수 있었던** 취약점을 수정(스코프별 분기 확인 로직 추가,
  실측 완료 - 프로젝트 키로 전역 템플릿 수정 시도 시 403 확인함).
- [x] 팀/그룹 스코프 템플릿 override가 실제로 그 산하 프로젝트에
  상속되는지 재확인 - 테스트 팀/그룹/프로젝트를 만들어 팀 스코프에
  override를 걸고 프로젝트 스코프 조회로 상속 확인, 그룹 스코프에도
  override를 걸어 팀보다 우선 적용되는지 확인(상속 우선순위: 프로젝트
  > 그룹 > 팀 > 전역) - 이번 세션에서 실측.
- [x] **`#template-history`: 템플릿 변경 이력** - 이미 override가
  있는 스코프를 다시 덮어쓸 때만(최초 override 생성 시엔 스냅샷할
  이전 값이 없으므로 제외) 덮어쓰기 직전 내용이 리비전으로 남는지,
  `editedBy`가 실제 로그인 사용자인지, 그 리비전 내용을 다시
  `template set`에 넘겨 복원하는 워크플로가 실제로 동작하는지 -
  CLI/API 왕복으로 전부 실측.

### 추가 개발 계획
현재 없음.

---

## 14. 가이디드 마이그레이션 (scan/apply)

### 사용 시나리오
- 설계자가 `concept` 브랜치 스타일의 옛 파일 기반 프로젝트를 이
  시스템으로 옮긴다 - 후보를 스캔해 매니페스트로 저장하고, 대상
  프로젝트에 맞게 다듬은 뒤 반영한다.

### QA 체크리스트
- [x] 실제 `concept` 문서 15개로 스캔 - `.tracking.json` 카운터와
  실제 파일 수 대조, index.md류 제외 확인 - 실측.
- [x] **이번 세션에서 새로 발견·수정 3건**: (1) Meilisearch
  write-through 경합으로 배치 내 링크 생성이 거의 다 실패하던 문제,
  (2) PowerShell `>` 리다이렉트가 남기는 UTF-8 BOM 때문에 `migrate
  apply`의 매니페스트 JSON 파싱이 깨지던 문제, (3) 같은 BOM 때문에
  `scanDirectory`가 옛 문서 파일을 **에러 없이 조용히** 후보에서
  빠뜨리던 문제 - 셋 다 재현 후 수정, 수정 전/후 대조까지 완료.
- [x] 대상 프로젝트에 없는 DocType(`RV`/`LG`/`DN`)은 `errors`로 보고,
  자동 생성 안 함 - 실측.
- [x] 옛 상태 어휘(`active`/`applied` 등)가 새 표준 6코드와 안 맞아
  상태 전이가 대부분 실패해 경고로 남고 초기 상태(draft) 유지 -
  의도된 동작으로 재확인(당시엔 프리셋이 없던 시점의 기록).
- [x] **상태 매핑 프리셋(`#migrate-status-mapping-preset`)** -
  `active`/`wip`는 자동으로 `approved`/`draft`로 제안되고
  `originalStatusCode`에 원본이 남는지, 프리셋에 없는 값은 그대로에
  그 필드 자체가 없는지 확인. `--no-status-preset`으로 완전
  옵트아웃되는지, 매핑된 값이 `migrate apply`로 실제 그 상태의
  문서를 만드는지 실측.
- [x] **`#migrate-idempotent`: 재실행 안전장치** - 같은 매니페스트를
  두 번 `apply`해도 문서가 중복 생성되지 않는지(1차는 `created`,
  2차는 전부 `alreadyApplied`로 나오고 문서 목록엔 여전히 원래
  개수만 있는지), 매니페스트 파일 자체가 `appliedTrackingCode`로
  갱신되는지, 항목 하나만 수작업으로 표시를 지우면 그 항목만 다시
  생성되고 나머지는 그대로 `alreadyApplied`인지(부분 재시도), 이미
  반영된 항목을 링크로 가리키는 아직 안 반영된 항목이 경고 없이
  정확히 그 trackingCode로 링크되는지, 실패한 항목은 표시가 안 남아
  재실행 시 자동으로 다시 시도되는지 - 전부 실측.

- [x] **`#adoption-migration-guide` 전체 절차 실측 리허설** -
  README.md/CLAUDE.md가 안내하는 시나리오 1(이미 `CLAUDE.md`가 있는
  기존 프로젝트) 절차를 처음부터 끝까지 실제로 실행: `concept`
  브랜치의 실제 frontmatter 문서(DC/DS/PL/SP 5건, 상호 링크 포함)로
  로컬 git 저장소 구성 → 백업 브랜치(`backup/pre-cnwk-migration`)
  생성 → `MIGRATION.md` 작성 → `project-create` → `git link` → 로컬
  브랜치 push → `migrate scan`으로 매니페스트 생성 → 대상 프로젝트에
  없는 DocType(DC/DS/PL/SP) 생성 → `migrate apply`(5건 전부 생성,
  상태 프리셋/미매핑 안전-실패/배치 내 링크 해석·배치 밖 링크 스킵
  경고까지 전부 의도대로 동작 확인) → 재실행으로 멱등성 재확인
  (전부 `alreadyApplied`) → `template deploy` + `git pull`로
  CLAUDE.md/SKILL.md 수신까지 왕복 완료. 이 리허설 과정에서 §11/§13의
  `auto_init` 버그를 발견·수정.

### 추가 개발 계획
현재 없음.

---

## 15. 웹 UI 전반 (레이아웃/네비게이션/읽기·편집/다이얼로그)

### 사용 시나리오
- 설계자가 브라우저로 로그인해 팀→그룹→프로젝트를 만들고, 프로젝트
  안에서 문서 탐색기 사이드바로 문서를 찾아 읽고 고친다.
- 설계자가 프로젝트 "설정" 탭 옆 "키 관리" 탭에서 프로젝트 키를
  발급하고, 1회 노출되는 값을 복사해 안전한 곳에 저장한다.

### QA 체크리스트
- [x] 로그인 → 팀/그룹/프로젝트 CRUD → 사이드바 문서 탐색기 → 문서
  읽기/편집 모드 전환 → 상태 콤보박스 → 삭제 - 여러 라운드에 걸쳐
  브라우저로 실제 클릭 검증.
- [x] 프로젝트 "키 관리" 탭, 팀 화면 "키 관리" 토글, 프로필 "API 키"
  카드(본인에게만 노출) - 이번 세션 실제 클릭 검증.
- [x] `/users/:id`와 `/projects/:id`가 라우트 파라미터 이름(`id`)을
  공유해 프로필 화면에서 사이드바가 깨지던 버그 발견·수정(route
  meta 기반으로 교정) - 실측.
- [x] XSS 새니타이즈(문서 본문에 `<script>` 삽입 시도) - 실측.
- [x] **`#responsive-dark-mode` 1단계(기반 구축)** - 사이드바 하단
  라이트/다크/시스템 토글로 순환 전환, `AppLayout.vue`/
  `ProjectHomeView.vue`/`DocumentsView.vue`/`DocumentEditorView.vue`
  가 실제로 다크 팔레트로 바뀌는지 브라우저로 확인. Monaco 에디터를
  "편집" 모드로 열어 `vs-dark` 테마가 실제로 적용되는지 확인(캔버스
  위젯이라 CSS만으로는 안 바뀌는 지점 - JS 연동 확인 핵심).
  `resize_window`(mobile, 375px)로 좁혀 셸 사이드바가 off-canvas로
  접히고 햄버거 토글로 열리는지(오버레이 배경 포함), 프로젝트 탭
  바가 가로 스크롤로 안 깨지는지 확인. 6개 모달 다이얼로그는 이미
  `width: min(Npx, 90vw)`로 반응형 대응이 돼 있던 걸 조사로 확인 -
  추가 작업 불필요.
- [x] **`#responsive-dark-mode-phase2`: 나머지 화면 색상 토큰화 완료** -
  1단계에서 안 건드렸던 칸반 보드/프로젝트 목록/소스 브라우저 등을
  다크 모드로 전환 후 브라우저로 재확인(칸반 컬럼/카드/배지, 문서
  목록 카드, 사이드바 폴더 트리 전부 다크 팔레트로 바뀌는지). 저장소
  전체 grep으로 남은 하드코딩 hex 색상이 의도된 것(항상 색이 있는
  버튼/배지 위의 `color: #fff`, `App.vue`의 토큰 선언 자체)뿐인지
  확인 - 46개 `.vue` 파일 전부 토큰 전환 완료.
- [x] **대량 문서 목록 미페이지네이션 지점 제거(`#large-list-pagination`)** -
  문서 5개(그중 하나는 본문에만 있는 고유 키워드) 프로젝트로 실측:
  Q&A "근거 추가" 다이얼로그(EntityPickerDialog, document kind)를
  열 때 전체 배열이 아니라 `/documents/page?...pageSize=50`만
  호출되는지, 검색어 입력 시 `/search?q=...`로 전환되고 본문 전용
  키워드로 매치된 문서도 결과에 남는지(클라이언트 재필터로 안
  사라지는지) 확인. 변경 추적의 "문서 버전 이력"도 네이티브
  `<select>` 대신 같은 다이얼로그를 재사용 - 선택/표시/선택 해제
  왕복 확인. 폴더는 트리 구조라 이번 범위 밖(별도 판단 필요).
- [x] **칸반 보드 터치 드래그 지원(`#kanban-touch-dnd`)** -
  `vuedraggable`(SortableJS) 전환 후 실측: 같은 컬럼 안 카드 순서
  변경/다른 컬럼으로 카드 이동/컬럼 자체 순서 변경이 전부 새로고침
  후에도 유지되는지, 카드 클릭 시 다이얼로그가 여전히 열리는지(드래그
  라이브러리가 클릭을 가로채지 않는지), 카드의 "숨기기" 버튼이
  드래그로 오인되지 않고 클릭으로 동작하는지, 존재하지 않는 분류로
  이동 시도 시 실패 후 원래 순서로 되돌아가는지, 다크 모드에서 드래그
  중 고스트(placeholder)가 표시되는지, 모바일 폭(375px)에서 햄버거
  메뉴/탭 바/카드 탭-오픈이 정상 동작하는지 확인. 드래그 중 텍스트가
  함께 선택되지 않는지(`user-select:none`), 보드 빈 배경을 마우스로
  드래그하면 좌우로 팬 스크롤되는지(터치는 기본 스크롤)도 확인. 카드
  상세 다이얼로그의 분류 변경 드롭다운(좁은 화면용 대안 경로)으로
  이동 시 보드에 즉시 반영되는지도 확인. 칸반 보드 영역이 뷰포트의
  남은 높이를 채워 가로 스크롤바가 화면 하단에 고정되는지(다른 탭은
  기존과 동일하게 동작해 회귀 없음)도 확인.

---

## 16. CLI/MCP 완전성 원칙 (대칭성, 의도적 예외)

### 사용 시나리오
- 클로드가 어떤 동작이 필요할 때, "CLI에는 있는데 MCP엔 없어서 못
  한다"는 비대칭을 절대 마주치면 안 된다(완전성 원칙).
- 반대로 코멘트/폴더/키 관리처럼 **의도적으로** AI 표면에서 뺀
  기능은, 그 이유를 SKILL.md에서 클로드가 스스로 확인할 수 있어야
  한다.

### QA 체크리스트
- [x] CLI 전체 명령(`docs --help` + 모든 하위 그룹) vs MCP `tools/list`
  (85개) 전수 대조 - 완전 일치, 의도적 예외 4가지(`auth
  register/login/logout`, `comment`, `folder`, `key` 관리)만 빠짐 -
  이번 세션에서 실측.
- [x] SKILL.md(+ seed-templates 동기화 사본)에 이 네 가지 예외의
  이유가 명시돼 있는지 - 실측.
- [x] `npm run audit:cli-mcp`(backend) - CLI 106개/MCP 98개 자동
  대조, 의도적 예외 8개(`auth register/login/logout`,`auth use-key`,
  `key create/list/revoke`, `git my-token`) 적용 후 불일치 0 확인.
  CLI/MCP 각각에 임시 항목을 추가해 스크립트가 정확히 잡아내고
  `exitCode=1`이 되는지 양방향 실측 - 이번 세션에서 실측.
- [x] **목록 명령 페이지네이션(`#list-pagination-options`)** - 배열을
  반환하는 CLI/MCP 명령 약 29개 전부에 `--page`/`--count`(MCP는
  `page`/`pageSize`) 추가. 대표 표본 실측: 문서 55건 프로젝트에서
  `docs list`(기존 `/documents/page` 재사용, Tier A) 옵션 생략 시
  기존과 동일한 배열, `--page 2 --count 20` 시 정확히 21~40번째
  문서(`total=55`), 마지막 페이지(15건)·범위 밖 페이지(빈 배열,
  에러 아님) 확인 - CLI 바이너리를 직접 실행해 옵션 파싱까지 왕복
  검증. 칸반 카드(신규 `/kanban/cards/page`, Tier B, DB
  skip/take)와 push 훅 큐(신규 라우트, 빈 목록에서도 `{items:[],
  total:0}` 형태)도 같은 패턴으로 확인. 문서 전문검색은 같은
  `/search` 라우트에 `page`/`pageSize`를 추가로 얹은 특수 케이스 -
  옵션 유무로 배열↔페이지 객체가 정확히 갈리는지 확인. `git tree`
  (Gitea Contents API 자체가 페이지를 지원 안 해 서버가 전체를
  받아온 뒤 자르는 in-memory 방식)와 `git log`(Gitea가 총 개수를
  안 줘서 `{items,hasMore}` 모양인 예외)도 실제 git 저장소를 연결한
  프로젝트로 각각 왕복 검증. `npm run audit:cli-mcp`로 CLI 옵션과
  MCP 파라미터가 전부 쌍으로 맞는지 확인(`key list`/`user list`는
  기존 설계대로 CLI 전용이라 MCP 쪽 제외) - 이번 세션에서 실측.

## 17. 코드 관계도 (Code Relation Graph)

### 사용 시나리오
- Claude가 여러 파일을 가로지르는 탐색 끝에 "X가 Y를 거쳐 Z를 호출
  하는 이유"를 알아내면, 다음 세션이 재탐색 없이 바로 찾아 쓸 수
  있게 그 발견을 관계로 기록해둔다.
- 문서의 추적코드로 그 문서를 구현한 소스 파일들을 찾거나, 반대로
  소스 파일에서 그 파일과 연관된 관계/문서를 찾는다.
- 같은 발견을 "인증 흐름"과 "이번 버그 추적" 등 서로 다른 상위
  개념 여러 개의 자식으로 동시에 걸어둔다(다중 부모).

### QA 체크리스트
- [x] **핵심 CRUD + bulk**(`#code-relation-graph`) - CLI로 관계
  생성 → 웹 UI 그래프에 바로 보이는지, MCP `relation_get`으로 동일
  데이터가 조회되는지(JSON `data` 필드 왕복 포함) 확인. bulk-add
  (CLI `--file` JSON, 유효 항목+검증 실패 항목 혼합)가 항목별 부분
  성공 결과를 정확히 반환하는지, bulk-remove(유효 id+존재하지 않는
  id 혼합)도 동일하게 확인.
- [x] **다중 부모 그래프**(단일 트리 아님) - 관계 하나에 서로 다른
  두 관계를 동시에 상위로 연결(`--parents id1,id2`) → `relation
  parents`가 둘 다 반환하는지, 웹 UI 그래프에서 두 부모로부터 오는
  엣지가 동시에 그려지는지 확인.
- [x] **순환 허용 + 안전한 순회** - 순환 엣지(A→B→A)를 의도적으로
  만들어 에러 없이 성공하는지 확인 → `relation descendants --depth
  10`이 무한 루프 없이 방문 집합으로 안전하게 끝나고 중복 없이
  나오는지 확인 → 웹 UI에서 같은 순환을 실제로 펼쳐도(vis-network)
  브라우저가 멈추지 않고 정상적으로 그려지는지(펼치기로 추가된
  노드가 화면 밖에 배치돼 안 보이던 버그를 실측 중 발견·수정 -
  `network.fit()` 재프레이밍 추가) 확인.
- [x] **연관 문서(다중, `--refs`)** - 실제 문서 2건을 만들어 관계
  하나에 동시에 연결 → `--ref`(정확 일치) 필터로 조회 확인, 존재하지
  않는 추적코드로 연결 시도 시 명확한 에러로 거부되는지(`CodeRelationRef`
  가 `Document.trackingCode`로의 실제 FK) 확인 → `update --refs`로
  전체 교체 확인 → 연관 문서 하나를 삭제하면 그 참조만 cascade로
  없어지고 관계 자체는 살아남는지 재조회로 확인. 웹 UI에서 "연관
  문서"가 추적코드 자동 링크로 클릭 가능한지, 문서 미리보기의
  "관계도에서 보기" ↔ 관계 상세의 추적코드 클릭이 서로 왕복되는지
  확인.
- [x] **설계자별 완전 독립** - 별도 계정으로 `relation list`/`get`
  둘 다 다른 설계자의 관계를 전혀 못 보는지(존재 자체 비노출),
  자기 관계를 남의 관계에 엣지로 연결하려는 시도가 거부되는지 확인.
- [x] **브랜치 스코프 + 삭제 시 일괄 정리(`#pr-workflow-branch-scope`)**
  - 실제 로컬 clone에서 `git checkout`으로 브랜치를 오가며 플래그 없이
    `relation list`가 자동으로 그 브랜치만 보여주는지, `--all-branches`
    가 전체를 보여주는지 확인 → 실제로 Gitea에서 그 브랜치를 삭제 →
    시스템 웹훅의 `delete` 이벤트로 그 브랜치의 관계가 실제로 일괄
    삭제되는지, 문서의 "연관 브랜치" 링크는 반대로 그대로 남아있는지
    (의도적 비대칭) 확인.
- [x] CLI/MCP 완전 대칭(`npm run audit:cli-mcp`, 새 예외 없음),
  SKILL.md(+ `seed-templates/SKILL.md` 동기화) 갱신 확인.

---

## 우선순위 제안

백로그 항목 간 우선순위는 [PLANS.md](PLANS.md)의 "우선순위 제안"
절에서 관리한다(중복 기재 방지 - 이 문서엔 절별 태그만 남김).

이 문서 자체는 "완료 로그"가 아니라 **계속 갱신되는 참고 자료**다 -
새 기능 라운드가 끝나면 해당 절의 시나리오/체크리스트를 갱신한다.
처리된 개발 계획 항목은 PLANS.md에서 지우고, 그 절의 태그 목록에서도
같이 뺀다.
